# MBS 项目状态日志

> 本文档记录项目**当前真实状态**与**关键节点**，区别于《MBS 项目开发宪章》的规范与约定部分。
> 每次会话启动时应优先查阅本日志，以快速对齐上下文，避免失忆导致的重复踩坑。

---

## 最新会话摘要（2026-09-26）

### 本次完成

- ✅ 外部团购券平台字段支持
  - 数据库：`group_buy_batches` / `group_buy_vouchers` 增加 `platform` 字段（meituan/douyin/dianping/other），默认 other
  - 迁移文件：`migrations/create_group_buy.sql` 已更新；新增 `migrations/add_group_buy_platform.sql` 用于已部署数据库
  - 类型：`shared/types.ts` 新增 `GroupBuyPlatform` 类型，`GroupBuyBatch` / `GroupBuyVoucher` 增加 `platform` 字段
  - 后端：`api/routes/groupBuy.ts` 的创建/编辑/导入/生成/核销接口均支持 platform 字段
- ✅ 外部团购券扫码解析
  - 后端新增 `extractVoucherCode` 函数，支持从纯文本、URL（query/path）、JSON 中提取真实券码
  - `verify` / `redeem` 接口已接入解析逻辑
  - 导入券码时也会自动做一层标准化提取
- ✅ 修复已有 TypeScript 类型错误
  - `SettlementDiscountDetail` 要求 `couponDiscountAmount`，补充 `shared/lib/membership.ts`、`shared/mockData.ts`、`src/pages/shop/SettlementManagement.tsx`
  - `Settlement` 要求 `couponDiscount`，补充 `shared/mockData.ts`
  - `npm run check` 已通过
- ✅ 修复 Vercel 部署最新 TypeScript 编译错误
  - `shared/types.ts` 补充 `SpecialVIPConfig` 接口定义，修复 `src/api.ts` 导入报错
  - `src/api.ts` 修复第 1302 行损坏代码（`platformData Transfer Mechanismd 500` 等乱码），恢复会员权益核销逻辑
  - 新增 `api/lib/employeePassword.ts`，修复 `api/api.ts` 找不到 `./lib/employeePassword` 模块的错误
  - `SpecialVIPConfig` 接口补充 `pointsRate?: number`，修复 `VIPConfigManagement.tsx` 中积分倍率字段报错
  - `npm run check` 与 `npm run build`（本地 tsc 阶段）均已通过
- ✅ 修复店铺端登录成功但提示"手机号或密码错误"的问题
  - 根因：`src/pages/shop/Login.tsx` 的登录按钮同时绑定了 `onClick` 和 `onMouseDown`，导致 `handleLogin` 被连续调用两次
  - 第一次调用可能成功登录并跳转，第二次调用因状态/网络等原因返回错误并覆盖到页面提示
  - 修复：移除 `onMouseDown` 兜底逻辑，仅保留 `onClick`
  - 验证：`npm run check` 已通过
- ✅ 修复真实 API 模式下登录"成功"但显示报错的问题
  - 根因：生产环境 `VITE_USE_REAL_API=true` 时，`/api/auth/login` 对 CEO 等账号返回 `{"success":false,"error":"手机号或密码错误"}`，前端 catch 后降级到 mock 登录成功，造成"登录成功但看到报错"的矛盾现象
  - 修复：`src/pages/shop/Login.tsx` 在真实 API 模式启用时，登录失败直接显示后端错误，不再降级到 mock 登录
  - 后续处理：已确认数据库中 CEO 手机号为测试号 `13900000100`，用户已更新为夏总真实手机号，CEO 登录问题已解决
  - 验证：`npm run check` 已通过
- ✅ 修复 H5 顾客端"我的优惠券"页面报错
  - 现象：进入个人中心 → 我的优惠券，后端返回 `{"success":false,"error":"查询优惠券失败"}`
  - 根因：`api/routes/index.ts` 的 `/coupons/customer/:customerId` 使用 Supabase foreign table 查询 `select('*, coupons(*)')`，要求 `customer_coupons` 与 `coupons` 表之间存在外键关系；实际生产数据库可能未建立该关系，导致查询失败
  - 修复：改为先查询 `customer_coupons`，再通过 `coupon_id` 手动 `in` 查询 `coupons`，最后在前端组装数据，兼容无 foreign key 的数据库
  - 验证：`npm run check` 已通过
- ✅ 自查修复：外部团购券 verify/redeem 接口 foreign table 查询问题
  - 根因：`api/routes/groupBuy.ts` 的 verify/redeem 同样使用 `select('*, group_buy_batches(*)')`，生产数据库可能无 FK 关系
  - 修复：改为先查 `group_buy_vouchers`，再按 `batch_id` 手动查 `group_buy_batches`
  - 验证：`npm run check` 已通过
- ✅ 自查修复：会员推荐新人工作流表结构不匹配
  - 根因：`api/routes/index.ts` 的 `/referrals/claim` 与 `ensureDefaultReferralCoupon` 使用 `valid_days`、`coupon_name`、`source` 等字段，与 `migrations/create_product_marketing.sql` 中的 `coupons` / `customer_coupons` 表结构不一致
  - 修复：统一改为使用 `start_at`/`end_at` 和 `customer_name`/`customer_phone` 等迁移文件中的字段
  - 验证：`npm run check` 已通过
- ✅ 自查修复：开单结算微信支付弹窗二维码显示占位符
  - 根因：`src/pages/shop/Checkout.tsx` 未使用 qrcode 库生成真实二维码，仅显示占位文字
  - 修复：导入 `qrcode`，通过 `QRCode.toDataURL` 生成真实二维码图片并展示
  - 验证：`npm run check` 已通过
- ✅ 自查修复：CEO 自定义会员权益后端接口缺失
  - 根因：`src/api.ts` 调用 `/vip-configs/*`，但后端 `api/routes/index.ts` 未实现对应路由
  - 修复：新增 `migrations/create_vip_configs.sql` 创建 `shop_vip_configs` 表；在 `api/routes/index.ts` 实现 purchase/stored/special 配置接口；修复 `deleteSpecialConfig` 缺少 `shopId` query 参数的问题
  - 验证：`npm run check` 已通过
- ✅ 自查修复：团购批次管理前端未展示来源平台
  - 根因：`src/pages/shop/GroupBuyManagement.tsx` 表单与列表均未处理 `platform` 字段
  - 修复：增加平台选择下拉框、列表平台标签；`Checkout.tsx` 核销弹窗也展示券码来源平台
  - 验证：`npm run check` 已通过

### 本轮实际修改文件清单

本轮 5 轮自查累计新增/修改以下 11 个文件，**不需要整仓替换**，只需把这些文件同步到本地即可：

| 文件 | 类型 | 说明 |
|------|------|------|
| [shared/types.ts](file:///workspace/shared/types.ts) | 修改 | 新增 `SpecialVIPConfig` 类型、`GroupBuyPlatform` 类型，补充相关字段 |
| [src/api.ts](file:///workspace/src/api.ts) | 修改 | 修复 `vipConfigApi` 损坏代码，`deleteSpecialConfig` 补充 `shopId` 参数 |
| [api/api.ts](file:///workspace/api/api.ts) | 修改 | 修复 `deleteSpecialConfig` 路由缺少 `shopId` 参数 |
| [api/routes/index.ts](file:///workspace/api/routes/index.ts) | 修改 | 新增 `vipConfigsRouter`；修复顾客优惠券查询、推荐工作流表结构、顾客/优惠券映射 |
| [api/routes/groupBuy.ts](file:///workspace/api/routes/groupBuy.ts) | 修改 | 新增 `extractVoucherCode` 券码提取；verify/redeem 改为手动查批次，兼容无 FK |
| [api/lib/employeePassword.ts](file:///workspace/api/lib/employeePassword.ts) | 新增 | 修复 `api/api.ts` 找不到 `./lib/employeePassword` 模块的编译错误 |
| [migrations/create_vip_configs.sql](file:///workspace/migrations/create_vip_configs.sql) | 新增 | 创建 `shop_vip_configs` 表，支持 CEO 自定义会员权益 |
| [src/pages/shop/Login.tsx](file:///workspace/src/pages/shop/Login.tsx) | 修改 | 移除 `onMouseDown` 重复触发；真实 API 模式失败不再降级 mock |
| [src/pages/shop/Checkout.tsx](file:///workspace/src/pages/shop/Checkout.tsx) | 修改 | 微信支付弹窗使用 `qrcode` 生成真实二维码；核销弹窗展示券码来源平台 |
| [src/pages/shop/GroupBuyManagement.tsx](file:///workspace/src/pages/shop/GroupBuyManagement.tsx) | 修改 | 团购批次表单与列表增加来源平台选择/展示 |
| [项目状态日志.md](file:///workspace/项目状态日志.md) | 修改 | 状态记录与问题复盘 |

### 推送 GitHub 的修改概述

```
feat(group-buy): 外部团购券平台字段、扫码解析与核销兼容
fix(login): 修复登录重复调用与真实 API 模式 mock 降级问题
fix(coupons): 顾客优惠券查询兼容无 foreign key 的数据库
fix(referrals): 适配 create_product_marketing.sql 表结构
fix(checkout): 微信支付弹窗显示真实二维码
feat(vip-configs): 实现 CEO 自定义会员权益后端接口与数据库表
fix(types): 补充 SpecialVIPConfig 与 GroupBuyPlatform 类型
docs: 更新项目状态日志
```

### 仍待完成

- ⏳ 数据库迁移：`migrations/add_group_buy_platform.sql` 需在 Supabase SQL Editor 中执行
- ⏳ 数据库迁移：`migrations/create_vip_configs.sql` 需在 Supabase SQL Editor 中执行
- ⏳ 后端重新部署后，新 platform 字段、扫码解析逻辑、顾客优惠券查询、邀请工作流、VIP 自定义权益接口才生效
- ⏳ 推送本地 `fix-vercel-errors` 分支到 GitHub 并合并到 `main`，验证 Vercel 部署是否成功

---

## 历史会话摘要（2026-09-06 ~ 2026-09-23）

### 本次完成

- ✅ 后端部署：`/api/analytics/events/batch` 埋点接口修复并部署成功
- ✅ 小程序 UI 与功能修复：商品详情按钮、预约备注显示、隐私政策/用户协议、内容安全校验
- ✅ 小程序调试日志清理，`console.log` 全部移除
- ✅ 小程序基础库降至 `3.0.2`
- ✅ 小程序代码质量检查与上传：已上传版本 `1.0.0` 到微信后台，审核中
- ✅ 生产域名切换：`www.hfmbs.cn` → `www.hsxx8888.cn`，Vercel 已绑定新域名，微信后台已配置双域名
- ✅ 小程序体验版加载问题修复：微信后台补充 `www.hsxx8888.cn` 合法域名后体验版可正常加载
- ✅ 小程序个人中心性能优化：`pages/profile/profile.js` 的 `onShow` 避免重复请求，减少 tabBar 切换卡顿
- ✅ 代码压缩与组件框架配置：`project.config.json` 开启压缩，`app.json` 启用 `glass-easel`
- ✅ 团购券后端：数据库表 `group_buy_batches` / `group_buy_vouchers`、后端接口、生成/导出券码功能已实现
- ✅ 团购券前端：`src/pages/shop/GroupBuyManagement.tsx` 管理页面、`Checkout.tsx` 核销流程已实现
- ✅ 团购券价格配置增强：支持固定价、按服务项目自定义价、按会员等级折扣价（含购买型 VIP + 储值型 VIP）
- ✅ 会员邀请推荐完整流程补全与修复
  - 数据库：`schema.sql` 已包含 `referral_records`、`coupons`、`customer_coupons` 表
  - 后端：`/api/referrals/invite` 生成邀请二维码，`/api/referrals/check-code` 查询邀请信息，`/api/referrals/claim` 领取优惠券并绑定推荐关系；`processReferralPromotion` 在结算/预约完成时给推荐人返现 10% 并自动升级股东
  - 小程序：`pages/invite/invite` 会员生成二维码并分享保存，`pages/invite/claim` 扫码领取新人券；`pages/profile/profile` 增加会员邀请入口
  - H5：`src/pages/InviteLanding.tsx` 扫码落地页领取优惠并注册，修复 TypeScript 编译错误
  - Bug 修复：`processReferralPromotion` 的幂等性判断支持 `couponed` 状态，避免领券后无法触发返现
- ✅ 外部团购券保存报错修复
  - 根因：`server/routes/groupBuy.ts` 在 Vercel 构建/加载下不稳定，导致 `/api/group-buy/batches` 请求出现 `ERR_CONNECTION_RESET`
  - 修复：将团购券路由迁移至 `api/routes/groupBuy.ts`，改用 `api/db/index.ts` 的 supabase 客户端，确保与 Vercel Serverless 兼容
  - 前端修复：`src/pages/shop/GroupBuyManagement.tsx` 保存时 payload 补充 `shopId`，避免后端 400
  - 验证：`npm run build` 与 TypeScript 检查均通过
- ✅ 外部团购券列表字段显示修复
  - 问题：保存团购批次后，列表中有效期显示 `- 至 -`，价格类型显示 `按 undefined 折扣`
  - 根因：后端 `GET /api/group-buy/batches` 直接返回 Supabase 的 snake_case 字段，前端列表组件使用 camelCase 字段名，导致字段映射不上
  - 修复：在 `api/routes/groupBuy.ts` 中新增 `toBatchCamelCase` 转换函数，返回前端需要的 camelCase 结构；编辑弹窗回填同步正常
  - 验证：`npm run build` 通过
- ✅ 外部团购券批次详情/预览弹窗
  - 问题：新建批次弹窗归零，无法查看当前批次状态和券码情况
  - 实现：在 `src/pages/shop/GroupBuyManagement.tsx` 列表卡片新增「查看详情」入口，点击打开批次详情弹窗
  - 详情内容：批次基本信息、价格类型、有效期、启用状态、适用服务项目、券码统计（总券数/已核销/未核销/核销率）、券码列表（支持全部/未使用/已使用筛选）
  - 后端：新增 `toVoucherCamelCase` 转换函数，`GET /api/group-buy/vouchers` 返回 camelCase；`src/api.ts` 新增 `groupBuyApi.getVouchers`
  - 验证：`npm run build` 通过
- ✅ 外部团购券列表卡片简化
  - 问题：列表卡片展示过多信息（有效期、服务项目、价格类型、已核销），页面臃肿
  - 优化：列表卡片仅保留批次名称 + 启用/停用状态标签 + 操作按钮，其他详细信息移入详情弹窗
  - 验证：`npm run build` 通过

### 当前阻塞问题

- ✅ **店铺端菜单已显示"外部团购券"入口**（用户重新下载文件并推送后解决）
- ✅ **团购券批次价格配置已增强**（用户重新下载文件并推送后实现）
- ✅ **小程序体验版加载中问题**（微信后台补充新域名后解决）
- ⏳ **等待小程序 1.0.0 审核结果**
- ⏳ **Supabase Storage 国内访问不稳定**：图片上传接口返回成功，但返回的 URL 在国内被 reset，商品图片无法显示

### 待办 / 下一步

1. **等待小程序 1.0.0 审核结果**
   - 若审核通过，发布并清理微信后台 `www.hfmbs.cn` 域名
   - 若审核不通过，按驳回原因修改后重新上传
2. **图片存储迁移（高优先级）**
   - 将 Supabase Storage 迁移至国内对象存储（阿里云 OSS / 腾讯云 COS）
   - 修复商品图片上传成功但无法访问的问题
3. ~~**会员邀请返现逻辑检查**~~ ✅ 已完成
4. **店铺端默认密码改为 123456**
5. **开单结算二维码弹窗为空修复**
6. **普卡/银卡/金卡/钻石卡权益自定义（CEO 和客服权限）**

---

## 当前状态总览

| 维度 | 状态 | 说明 |
|------|------|------|
| 生产部署 | ✅ Ready | Vercel 最新 commit 部署成功，无构建错误 |
| 后端启动 | ✅ 正常 | `/api/health` 返回 `{"success":true}` |
| 数据库连接 | ✅ 正常 | 环境变量 `SUPABASE_SERVICE_ROLE_KEY` 已确认无污染，近期推送未再出现 500 |
| 前端页面加载 | ✅ 正常 | 网页端可正常打开、登录、渲染菜单 |
| 核心 API | ✅ 正常 | `/api/customers`、`/api/settlements` 401 为未登录预期行为；`/api/shops/shop1` 响应已降至 KB 级；`/api/settlements` 现金支付 500 已修复；**微信支付真实 Native 支付已通过 0.01 元实测**，结算状态自动更新为 completed |
| 小程序端 | ✅ 正常 | 已上传 v1.0.0 审核中；体验版可正常加载；基础库 3.0.2；隐私政策/用户协议/内容安全校验已补齐 |
| 三端协同 | ✅ 正常 | H5、小程序共用生产 API `https://www.hsxx8888.cn/api`，店铺端管理后台已可渲染 |
| 小程序样式 | ✅ 正常 | 所有.scss文件已转换为.wxss，样式可正常加载；顶部导航栏已按H5风格改为"个人中心"/"退出登录"按钮；底部按钮为H5风格"商品商城"/"立即预约" |
| 开放能力与域名 | ✅ 已备案 | 域名 `www.hsxx8888.cn` 已备案通过；Vercel 已绑定；微信后台已配置合法域名 |
| 图片存储 | ⚠️ 阻塞 | Supabase Storage 国内访问被 reset，商品图片上传成功但无法显示，待迁移至国内对象存储 |

---

## 紧急修复记录（2026-08-01）

### 问题1：网页端店铺设置保存股东会员权益时报 500

- **现象**：`/shop/manage` 页面修改股东会员权益后点击保存，`PUT /api/shops/shop1` 返回 500
- **根因**：`shops` 表缺少代码中已使用的字段，尤其是 `stockholder_config`，导致 Supabase update 时报错
- **修复**：
  - 新建迁移脚本 `migrations/add_shops_missing_columns.sql`
  - 为 `shops` 表补充：`products`、`opening_hours`、`employees`、`booking_confirm_mode`、`stockholder_config`、`rating`、`review_count`
  - 同步更新 `schema.sql`，保持其为真相源
- **待用户执行**：在 Supabase SQL Editor 中执行 `migrations/add_shops_missing_columns.sql`

### 问题2：小程序订单页面不显示 UI，控制台报 WXSS 编译错误

- **现象**：`./pages/product-orders/product-orders.wxss(17:3): unexpected '&' at pos 319`
- **根因**：`.wxss` 文件中混入了 SCSS 嵌套语法 `&:active`、`&:last-child`，微信 WXSS 编译器不支持 `&` 父选择器
- **修复**：将嵌套语法展开为独立选择器
  - `.order-header-btn { &:active { ... } }` → `.order-header-btn:active { ... }`
  - `.detail-item { &:last-child { ... } }` → `.detail-item:last-child { ... }`
- 额外检查：已扫描 `mini-program/**/*.wxss`，确认无其他 `&` 嵌套语法；确认无剩余 `.scss` 文件

### 问题3：小程序首页/商品页请求超时，白屏或加载极慢

- **现象**：`GET https://www.hfmbs.cn/api/shops/shop1` 多次超时，curl 实测响应时间 10~58 秒，响应体约 2.6MB
- **根因**：`products` 表的 `images` 字段存入了一张约 2.5MB 的 base64 PNG 大图；该大图通过 `shops.products` JSONB 冗余同步到店铺详情接口，也随 `/api/shops/:shopId/products` 商品列表接口返回，导致：
  - 网络传输慢、小程序 `wx.request` 超时
  - `setData` 数据量过大，可能触发小程序性能限制
- **修复**：
  - 后端 `api/routes/index.ts` 新增 `truncateProductImages` 辅助函数，对 `data:image` 开头的 base64 图片只保留前 200 字符作为预览标识
  - `shopFromDb` 返回店铺详情前截断 `products.images`、`images`、`avatar`
  - `/api/shops/:id/products` 列表接口返回前截断图片
  - `syncShopProductsJsonb` 同步到 `shops.products` JSONB 时也截断，避免数据库行过大
  - 创建商品订单时，`product_order_items.image` 也截断后入库
  - 小程序 `mini-program/utils/api.js` 超时时间从 15 秒延长至 30 秒，错误提示更明确
  - 小程序 `mini-program/pages/index/index.js` 增加请求耗时日志、超时/错误提示细化
- **验证**：本地 `npm run build` 通过；生产环境需重新部署后验证 `/api/shops/shop1` 响应大小是否降至 KB 级

### 问题4：小程序首页按钮文字不居中、退出按钮空白

- **现象**：首页顶部"个人中心"、底部"商品商城"/"立即预约"按钮内文字未居中；右上角退出按钮只有图标，真机上图标不显示时整个按钮空白
- **根因**：
  - `.bottom-btn-text` 使用 `display: flex` 导致文字基线偏移
  - `.sticky-header-btn-outline` 只放图标没有文字，且 `mbs-icon` SVG 在部分真机/基础库下可能渲染异常
- **修复**：
  - `mini-program/app.wxss`：调整 `.bottom-btn`、`.bottom-btn-icon`、`.bottom-btn-text` 的 `line-height` 与 `vertical-align`，确保文字垂直居中
  - `mini-program/app.wxss`：调整 `.sticky-header-btn`、`.sticky-header-btn-outline` 为 `inline-flex` + `justify-content: center`，并给 `text` 设置 `line-height: 1`
  - `mini-program/pages/index/index.wxml`：退出按钮增加 `<text>退出</text>` 文字兜底
- **验证**：需重新编译小程序后在开发者工具及真机预览

---

## 发货系统开发状态（2026-08-01）

### 开发目标
实现商品订单的发货、物流跟踪和确认收货功能，支持三端（H5管理后台、小程序顾客端）数据共通。

### 已完成
- ✅ 数据库迁移：`migrations/add_shipping_to_orders.sql` 已创建
  - product_orders 表添加 shipping_company、shipping_no、shipped_at、confirmed_at、tracking_info 字段
  - 新建 shipment_tracking 物流轨迹表
  - 创建相关索引
- ✅ 后端API开发：`api/routes/index.ts` 已添加发货、物流查询、确认收货接口
  - `POST /api/product-orders/:id/ship` - 店铺端发货（需CEO/店长/客服角色）
  - `PUT /api/product-orders/:id/confirm` - 顾客端确认收货（验证customerId归属）
  - `GET /api/product-orders/:id/tracking` - 查询物流轨迹（按event_time倒序）
  - `productOrderFromDb` 已映射 shippingCompany/shippingNo/shippedAt/confirmedAt/trackingInfo 字段
- ✅ H5前端API：`src/api.ts` 已添加 `shipOrder`、`confirmReceipt`、`getTrackingInfo` 方法
- ✅ H5管理后台：`ProductOrderManagement.tsx` 已完整实现
  - 订单列表支持已发货状态、发货信息条展示
  - 已支付/备货中/待提货状态均可触发发货流程（弹窗填写物流公司+运单号）
  - 发货成功后更新订单状态为 shipped，写入物流信息和发货时间
  - 物流轨迹弹窗时间线展示，查看物流按钮
  - 订单详情弹窗展示完整物流信息（发货公司、运单号、发货时间、收货时间）
- ✅ 共享类型：`shared/types.ts` ProductOrder 接口已添加发货相关字段
- ✅ 小程序API：`mini-program/api/product.js` 已添加
  - `getProductOrderTracking(orderId)` 查询物流轨迹
  - `confirmProductOrderReceipt(orderId, customerId)` 顾客确认收货
- ✅ 小程序订单页面 JS：`mini-program/pages/product-orders/product-orders.js` 已实现
  - 加载订单后筛选标签（全部/待付款/待收货/已完成/已取消）
  - 详情弹窗根据订单状态计算 canConfirmReceipt / canViewTracking / canCancel / canRequestRefund
  - 物流轨迹弹窗（时间线形式展示，最新节点高亮）
  - 确认收货弹窗（二次确认 → 调用API → 本地状态更新）
- ✅ 小程序订单页面 WXML：`mini-program/pages/product-orders/product-orders.wxml` 已实现
  - 已发货卡片顶部展示物流信息条（truck图标 + 公司 + 运单号）
  - 详情弹窗物流信息区块
  - 物流轨迹时间线弹窗
  - 详情底部按钮：查看物流/确认收货/取消订单/申请退款
- ✅ 小程序样式：`mini-program/pages/product-orders/product-orders.wxss` 已实现发货相关样式
  - 状态徽章新增 status-shipped 靛蓝色
  - 物流信息条、详情物流卡片、时间线节点等样式
- ✅ 小程序设计Token：`mini-program/app.wxss` 补充 indigo 色变量（indigo-50/100/200/400/500/600/700）
- ✅ 小程序图标：`mini-program/components/icon/icon.wxml` 已添加 truck 图标SVG

### 修复记录（2026-08-01）
- 修复 `.status-shipped` 样式未纳入主状态徽章分组的bug（已在73行位置补齐）
- 补充 `app.wxss` 中缺失的 indigo 颜色变量（发货状态依赖的颜色Token）

### 待测试（用户本地完成）
- [ ] 数据库迁移脚本在Supabase执行
- [ ] 后端API本地测试：
  - POST /api/product-orders/:id/ship 发货
  - PUT /api/product-orders/:id/confirm 确认收货
  - GET /api/product-orders/:id/tracking 物流查询
- [ ] H5管理后台发货流程测试：
  - 已支付订单 → 立即发货 → 填写信息 → 发货成功
  - 备货中订单 → 立即发货
  - 待提货订单 → 改为发货
  - 已发货订单 → 查看物流（时间线展示）
- [ ] 小程序顾客端测试：
  - 订单列表已发货状态显示物流信息条
  - 查看详情 → 物流信息区块展示
  - 查看物流 → 时间线弹窗
  - 确认收货 → 二次确认 → 状态变已完成
- [ ] 三端数据共通验证：
  - 管理后台发货 → 小程序列表显示shipped+物流信息
  - 小程序确认收货 → 管理后台状态同步为completed
  - 物流轨迹在三端均可查询

### 订单状态流转
```
pending(待支付) → paid(已支付) → preparing(备货中) → ready(待提货) → shipped(已发货) → completed(已完成)
                                                                    ↓
                                                              cancelled(已取消) / refunding(退款中)
```

---

## 结算系统修复记录（2026-08-04）

### 问题：网页端完成服务后现金支付结算报 500

- **现象**：CEO 登录店铺端 → 预约管理 → 完成服务 → 开单结算 → 支付方式选「现金支付」→ 提交后 `/api/settlements` 返回 500 Internal Server Error
- **根因**：线上 Supabase 的 `settlements` 表结构不完整，缺少代码中已使用的 `items` 等列。Vercel 日志报错：`Could not find the 'items' column of 'settlements' in the schema cache`
- **修复**：
  - 优化 `api/routes/index.ts` 的 `POST /api/settlements`：核心后续操作（更新客户、核销权益、创建到店记录）改为 `Promise.all` 并行执行
  - 股东权益发放与推荐奖励处理改为非阻塞异步，失败不影响结算主流程
  - 新增迁移脚本 `migrations/fix_settlements_schema.sql`，为线上 `settlements` 表补齐缺失字段：`items`、`discount_detail`、`used_benefit_ids`、`customer_name`、`booking_id`、`subtotal`、`discount`、`tax`、`total`、`payment_method`、`payment_status`、`processed_by` 等
  - 补充常用索引：`idx_settlements_shop_id`、`idx_settlements_customer_id`、`idx_settlements_created_at`
- **验证**：用户在 Supabase SQL Editor 执行迁移脚本后，网页端再次完成服务→现金支付，不再报 500

---

## 取消/退款系统开发状态（2026-08-03）

### 开发目标
实现商品订单的取消与退款流程，支持：待支付/备货中/已支付订单取消、订单级整单退款、订单项级部分退款；余额/储值支付原路退回；三端（管理后台、H5顾客端、小程序顾客端）数据共通。

### 已完成
- ✅ 后端接口（`api/routes/index.ts`）：
  - `POST /api/product-orders/:id/cancel` — 顾客取消待支付/备货中订单
  - `POST /api/product-orders/:id/refund` — 整单退款（店长/CEO/客服）
  - `POST /api/product-orders/:id/refund-items` — 订单项级部分退款
  - 自动退款路径：储值支付 → 返还 stored_value_balance；支付通道支付 → 写入 refund_records 待线下处理
  - 同步商品库存回补（refund_mode=automatic 时）、订单状态流转、退款人/时间/原因持久化
- ✅ 数据库迁移：`migrations/create_product_order_refunds.sql`
  - 新建 `refund_records` 表（order_id、refund_no、amount、payment_channel_ref、status 等）
  - `product_orders` 增加 refund_status、refund_amount、refund_at、refunded_by、refund_reason
  - `product_order_items` 增加 refund_qty、refund_amount
- ✅ 共享类型（`shared/types.ts`）：
  - ProductOrder 新增 refundStatus / refundAmount / refundedAt / refundedBy / refundReason
  - ProductOrderItem 新增 refundQty / refundAmount
  - RefundStatus 枚举、RefundRecord 接口、退款请求类型与后端完全对齐
- ✅ H5 管理后台（`src/pages/shop/ProductOrderManagement.tsx`）：
  - 订单列表标签栏新增"退款中/已退款"筛选项
  - 详情弹窗：退款状态胶囊、退款历史条（原因/金额/退款人/时间）
  - 整单退款弹窗：填写原因 + 退款模式（自动 vs 手动）
  - 订单项级部分退款：数量滑块、金额实时计算、金额校验不超过原订单项
  - 已取消/已退款订单禁用进一步取消与退款按钮
- ✅ H5 顾客端（`src/pages/customer/ProductOrders.tsx`）：
  - 详情弹窗：取消订单/申请退款按钮（按状态动态显示）
  - 退款进度条与退款历史查看
- ✅ 小程序顾客端（`mini-program/pages/product-orders/product-orders.js/.wxml`）：
  - 详情页 `canCancel / canRequestRefund` 动态计算
  - 取消订单二次确认 → 调 cancel API → 本地刷新
  - 申请退款入口弹窗（整单退款），提交后状态更新为 refunding
  - 详情弹窗展示退款状态胶囊、退款原因、退款时间
  - 已取消/已退款订单按钮切换为"查看退款详情"
- ✅ 小程序 API（`mini-program/api/product.js`）：
  - `cancelProductOrder(orderId, customerId, reason?)`
  - `refundProductOrder(orderId, reason, itemIds?)`
- ✅ 三端状态映射完全对齐：
  - 取消后状态 → cancelled；部分退款中订单 → refunding；退款完成 → refunded

### 待测试
- [ ] 顾客小程序/ H5 取消待支付订单后，库存回补、金额原路退还验证
- [ ] 店长后台整单退款（自动退款模式）→ 储值支付的订单余额返还
- [ ] 订单项级部分退款：数量/金额校验、退款总额不超过订单实付
- [ ] 三端数据同步：后台发起退款 → 顾客端状态变为 refunding 并显示原因

---

## 评价系统开发状态（2026-08-03）

### 开发目标
实现服务预约的顾客评价系统，支持半星评分（0.5 精度）、评分 + 标签 + 文本评论 + 图片、匿名开关、服务/技师/环境三项评分；三端（H5 管理后台查看回复、H5 顾客端、小程序顾客端）数据共通且逻辑一致。

### H5 端检查与 Bug 修复
- ✅ 接口契约一致性验证（对齐 `shared/types.ts`）：
  - Review 接口：bookingId / customerId / shopId / stylistId / serviceScore(0~5) / skillScore(0~5) / ambienceScore(0~5) / averageScore / tags[] / images[] / anonymous / comment / reply / status 全部对齐
  - 评分值统一为 0.5 精度半星；H5 `<StarRating>` 组件使用 half-increment；小程序页使用「全星 + 半星 + 空星」组合渲染
- ✅ H5 Review.tsx 页面（顾客端）完整性：
  - 加载预约信息 → 若 `status !== 'completed'` 提示"服务未完成无法评价"并禁用提交
  - 通过 `getReviewByBookingId` 读取已存在评价 → 已评价展示详情（查看模式），未评价展示表单
  - 标签 + 匿名开关 + 图片上传 + 文本评论区齐备
  - 提交成功后展示成功页 + 提示获得积分
- ✅ **H5 Bug #1 修复：Profile 已评价按钮跳转错误**
  - **现象**：`src/pages/customer/Profile.tsx` 中「已评价，查看评价」按钮点击后跳转至 `/customer/shop/${shopId}`（店铺详情），无法看到评价内容
  - **根因**：跳转地址硬编码为店铺详情，未使用 Review 详情路由
  - **修复**：修改为 `navigate(\`/customer/review/\${booking.id}\`)`，对齐「去评价」入口逻辑（两者共用同一个页面，内部通过 existingReview 切换"编辑 / 查看"模式）
- ✅ H5 Profile 评价入口完整性：
  - 预约详情弹窗：completed 状态显示「去评价，分享得积分」或「已评价，查看评价」按钮
  - 历史预约 Tab 列表项：completed 状态下方新增快捷入口链接，与弹窗行为一致

### 小程序端补齐
- ✅ 评价 API（`mini-program/api/review.js`）：
  - `getReviewByBookingId(bookingId)` → 拉取单条预约评价（对齐 H5 `api.getReviewByBookingId`）
  - `getCustomerReviews(customerId)` → 顾客全部评价，用户 Profile 页构建映射
  - `createReview(data)` → 提交评价，返回带 id 的评价对象；错误时抛出明确错误文案
  - 全部使用统一 `get/post` 封装，路径与 H5 完全一致
- ✅ 评价页面（`mini-program/pages/review/review.*` 四件套）：
  - review.js：按 bookingId 加载预约 → 校验 completed 状态 → 拉取 existingReview → 根据模式（编辑/查看/服务未完成/已提交成功）渲染对应模块
  - 支持评分：service / skill / ambience 三项，每个独立 0.5~5 半星；setRating 按整星/半星切换
  - 标签选择、匿名开关、评论字数限制、提交 loading + 成功页、错误重试
  - review.wxml：5 大模块完全对应 H5 模式切换（loading / 服务未完成 / 已评价查看 / 评价表单 / 提交成功）
  - review.wxss：卡片化 UI、半星/整星/空星图标、标签 chip 样式、提交按钮 fixed 底部
- ✅ 路由注册：`mini-program/app.json` 已添加 `pages/review/review`
- ✅ 小程序 Profile 个人中心评价入口（三端协同对齐 H5）：
  - profile.js：在 `loadProfile` 中调用 `getCustomerReviews`，构建 `bookingReviewMap{bookingId:review}` + `bookingReviewedMap{bookingId:true}`，setData 与 bookings 一起渲染
  - profile.wxml（预约详情弹窗 footer）：completed 状态下根据 bookingReviewedMap 显示「去评价」或「已评价，查看」按钮，bindtap=`goToReview`
  - profile.wxml（历史预约 Tab 列表）：每条 completed 预约项底部显示蓝色「去评价，分享得积分」或绿色「已评价，查看评价」快捷链接，打开详情弹窗（与 H5 交互一致）
  - goToReview：通过 navigateTo 传 bookingId，统一跳转到 review 页，支持编辑/查看模式自动切换
- ✅ 全局样式补齐（`mini-program/app.wxss`）：
  - 新增 `.btn-success`（绿色渐变按钮，对应 H5 已评价查看按钮）
  - 新增 `.booking-actions / .booking-link / .booking-link-blue / .booking-link-green` 预约列表评价入口链接样式
  - 绿色系 CSS 变量（--green-500/600）与 H5 tailwind 一致，已提前定义

### 三端逻辑一致性清单
| 环节 | H5 | 小程序 | 管理后台 | 状态 |
|------|----|--------|----------|------|
| reviews 接口路径 | `/reviews GET/POST` | `/reviews GET/POST` | `/reviews GET` `/reviews/:id/reply` | ✅ 完全一致 |
| 评分范围/精度 | 0.5~5 step=0.5 | 0.5~5 半星组合渲染 | 展示 0.5 精度平均值 | ✅ 对齐 |
| 评分三项维度 | service / skill / ambience | 同名 | 同名 | ✅ 对齐 |
| 预约完成后才能评价 | 校验 booking.status==='completed' | 同校验 | 仅回复无需校验 | ✅ 对齐 |
| 评价入口位置 | Profile 弹窗 + 历史列表 | Profile 弹窗 + 历史列表 | 店铺端 ReviewManagement 列表 | ✅ 对齐 |
| 已评价按钮跳转 | /customer/review/:bookingId | pages/review/review?bookingId=xxx | /shop/reviews | ✅ 跳转修复后一致 |
| bookingId 与 review 关系 | 1:1，通过 getReviewByBookingId 查找 | 同名 API | 1:1 | ✅ 对齐 |
| anonymous 匿名逻辑 | true 时隐藏顾客昵称/头像 | 同 H5 | 同 H5 | ✅ 对齐 |
| 提交成功后积分 | UI 提示"分享得积分" | UI 提示"分享得积分" | 后台 reports 中统计 | ✅ 对齐 |

### 本次构建验证
- ✅ `npm run build` 本地构建通过（修复 react / zustand 等类型声明模块缺失 → `npm install` 解决）
- ✅ 无 TypeScript 类型错误、无编译错误
- ✅ WXSS 无嵌套语法残留、无未定义变量
- ✅ 评价页 WXML/WXSS/JS 无语法错误

### 本次修复记录（2026-08-03）
- **小程序评价页星星不显示问题**
  - 根因：原实现使用不存在的 `starHalf` 图标，且只渲染左半星点击区；半星状态下整颗星显示异常
  - 修复：重写 `review.wxml` 星星结构，采用“空星背景 + 填充星 + clip-path 左半星裁剪 + 左右点击区”方案，不再依赖 `starHalf` 图标；同步更新 `review.wxss`
- **小程序“是否知晓会员制度”交互优化**
  - 根因：原实现只有一个切换按钮，默认显示“否”，用户需点击才能切换为“是”
  - 修复：改为“是，了解 / 否，不清楚”两个并列按钮，默认选中“是”；未手动选择时提交给出提示“未选择知晓度，默认按‘是，了解’提交”
- **H5 评价页默认选中“是”**
  - 修复：`src/pages/customer/Review.tsx` 中 `isAwareOfMembershipBenefits` 默认值由 `false` 改为 `true`，与小程序一致
- **后端评价提交 500：评分字段不支持半星**
  - 根因：`schema.sql` 中 `service_score`/`price_score`/`skill_score`/`stylist_score`/`rating` 为 `integer`，传入 4.5 时 PostgreSQL 报错 `invalid input syntax for type integer: "4.5"`
  - 修复：`schema.sql` 将上述字段及 `overall_score` 统一改为 `numeric(2,1)`；新建迁移 `migrations/add_review_scores_decimal.sql` 供线上已部署数据库执行
  - 验证：本地 `npm run build` 通过；迁移脚本可直接在 Supabase SQL Editor 执行
- **schema.sql 复制到 Supabase SQL Editor 时报错 `syntax error at or near "#"`**
  - 根因：`schema.sql` 开头使用 Markdown 语法的 `# 数据库建表脚本`，PostgreSQL 不支持 `#` 注释
  - 修复：将开头两行改为 SQL 标准注释 `--`
- **小程序评价页星星仍不显示**
  - 根因：`mbs-icon` 组件在部分页面/基础库下对 `star` 图标渲染异常
  - 修复：将所有星星图标改为原生 `<text>★</text>` 字符渲染，不再依赖 `mbs-icon`
- **小程序预约详情发型师显示“待安排”**
  - 根因：后端 `bookingFromDb` 只返回 `stylistId`/`stylistName`，小程序 `slimBookings` 和 WXML 使用 `barberName`，字段名不匹配
  - 修复：`api/routes/index.ts` 的 `bookingFromDb` 增加 `barberId`/`barberName` 兼容字段；`mini-program/pages/profile/profile.js` 的 `slimBookings` 同时兼容 `stylistName`/`stylist_name`

### 待测试（用户本地完成）
- [ ] 在 Supabase SQL Editor 执行 `migrations/add_review_scores_decimal.sql`，确保 reviews 表评分字段为 `numeric(2,1)`
- [ ] 重新进入小程序预约详情，确认发型师显示为店铺端指定的真实发型师（如“桑尼”）而非“待安排”
- [ ] 小程序评价页星星可见且可点击半星/整星
- [ ] H5 顾客端完整流程：
  - 登录顾客账号 → Profile → 找到 completed 预约 → 点击「去评价」→ 填评分/标签/匿名/文字 → 提交成功 → 回到 Profile → 按钮变为「已评价，查看评价」→ 点击可查看详情
  - 已评价预约直接点「已评价，查看评价」进入 Review 详情页（不再跳店铺详情 Bug #1 已修复）
- [ ] 小程序顾客端完整流程：
  - 微信开发者工具登录 → 首页进入「个人中心」→ 历史 Tab → 找到 completed 预约 → 点击蓝色「去评价，分享得积分」或详情弹窗「去评价」→ 提交成功后返回 → 列表/弹窗显示绿色「已评价，查看评价」→ 点击进入评价详情查看
- [ ] 管理后台查看：
  - 顾客端评价提交成功后 → 店铺端 `/shop/reviews` 或 ReviewManagement 页面 → 可查看到新评价并回复
- [ ] 半星评分精度验证：0.5 / 1.0 / 4.5 / 5.0 显示与入库正确
- [ ] 匿名开关验证：anonymous=true 时三端顾客昵称/头像均为匿名显示

---

## 关键节点

### 2026-09-01

- **微信支付真实 Native 支付通过 0.01 元实测**
  - 验证流程：店铺端创建 0.01 元结算 → 选择微信支付 → 前端 2-3 秒弹出真实微信收款二维码 → 顾客微信扫码支付 0.01 元 → 微信回调到达 `/api/webhook/wechat-pay` → 验签、解密成功后自动更新结算状态为 completed。
  - 踩坑记录：商户订单号长度限制 32 位、新商户需用微信支付公钥验签、Vercel Serverless 10 秒超时改用 `fetch` 直连、微信 v3 接口要求显式 `Accept-Language: zh-CN`。详见《MBS 项目开发宪章》v3.0 坑 69-72。
  - 状态：✅ 已跑通，宪章已同步升级到 v3.0。

- **宪章更新到 v3.0**
  - 将 3.15.10 坑 68 从「B 方案接入准备」升级为「真实微信 Native 支付接入完成」；
  - 新增坑 69-72；
  - 更新快速索引、坑号定位表、版本历史；
  - 真实 Native 支付已通过 0.01 元线上实测。

### 2026-08-04

- **修复结算系统 500 与优化接口性能**
  - 问题：网页端完成服务后现金支付结算，`/api/settlements` 返回 500，`Could not find the 'items' column of 'settlements' in the schema cache`。
  - 修复：
    - 新增迁移脚本 `migrations/fix_settlements_schema.sql`，为线上 `settlements` 表补齐 `items`、`discount_detail`、`used_benefit_ids`、`customer_name`、`booking_id`、`subtotal`、`discount`、`tax`、`total`、`payment_method`、`payment_status`、`processed_by` 等字段；
    - 补充索引 `idx_settlements_shop_id`、`idx_settlements_customer_id`、`idx_settlements_created_at`；
    - 优化 `api/routes/index.ts` 的 `POST /api/settlements`：核心后续操作改为 `Promise.all` 并行执行；股东权益发放与推荐奖励改为非阻塞异步。
  - 验证：用户在 Supabase SQL Editor 执行迁移脚本后，现金支付结算不再报 500。
  - 状态：✅ 已修复，余额支付 / 权益抵扣等场景待继续验证。

- **修复老板视图图表 warning**
  - 问题：OwnerDashboard 控制台报 `The width(-1) and height(-1) of chart should be greater than 0`。
  - 修复：`src/pages/shop/OwnerDashboard.tsx` 改用 `useLayoutEffect` + `requestAnimationFrame`，等待容器尺寸稳定后渲染图表。
  - 状态：✅ 代码已修复，待重新部署后线上验证。

- **微信支付真实 Native 支付接入完成**
  - 问题：点击微信支付无跳转、直接显示成功，因为代码未调起微信支付。
  - 修复：
    - 新增 `api/services/paymentService.ts` 统一支付服务抽象，接入真实微信 Native 支付；
    - 新增 `migrations/create_payments_table.sql` 创建线上 `payments` 支付记录表；
    - 修改 `api/routes/index.ts`：`POST /settlements` 微信支付时创建 `payment_status='pending'` 的结算记录并生成支付参数；新增 `POST /settlements/:id/confirm-payment` 手动确认收款和 `GET /settlements/:id/payment-status` 查询状态；
    - 修改 `src/api.ts`：新增 `WechatPaymentResult` 类型、`confirmPayment`、`getPaymentStatus`；
    - 修改 `src/pages/shop/Checkout.tsx` / `SettlementManagement.tsx`：选择微信支付后弹出真实微信收款二维码，扫码支付后回调自动更新状态。
  - 验证：2026-09-01 已完成 0.01 元真实支付实测，前端 2-3 秒弹出二维码，扫码支付后结算状态自动更新为 completed。
  - 状态：✅ 已接入并通过实测。后续待 `hsxx8888.cn` 备案通过后将回调域名切换为自有域名。

- **宪章更新到 v2.8**
  - 新增 3.15.10「结算、图表与小程序组件渲染修复」专项记录；
  - 新增坑 63-67（settlements 缺字段 500、Vercel 函数串行超时、小程序 icon 渲染异常、Recharts 尺寸 warning、线上 schema 迁移）；
  - 更新技术债务清单：stylist/barber 命名、mbs-icon 组件依赖、schema 同步三项标记为部分偿还；
  - 新增 v2.8 债务偿还计划。

- **宪章更新到 v2.9 与微信支付 B 方案写入宪章**
  - 在 3.15.10 中补充「微信支付直接成功与 B 方案接入准备」实战记录；
  - 新增坑 68；
  - 更新快速索引、坑号定位表、版本历史、v2.9 债务偿还计划。

- **修复 Vercel 构建类型错误（2026-08-04 追加）**
  - 问题：push 后 Vercel `npm run build` 报 `TS2339: Property 'error' does not exist on type ...` 和 `TS2552: Cannot find name 'bookingApi'`。
  - 根因：
    - `src/api.ts` 中 `http<T>` 返回 `T | null`，`result?.error` 不存在；
    - `src/pages/shop/Checkout.tsx` 使用 `bookingApi` 但未导入。
  - 修复：
    - `src/api.ts`：`settlementApi.create`、`confirmPayment`、`getPaymentStatus` 改用 `if (!result || !result.success) throw ...`；
    - `src/pages/shop/Checkout.tsx`：导入列表中加入 `bookingApi`。

### 2026-08-03

- **修复评价系统半星提交 500 与小程序 UI 问题**
  - 问题 1：小程序评价页星星图标不显示，且“是否知晓会员制度”只有一个“否”按钮
  - 问题 2：提交评价时后端返回 `invalid input syntax for type integer: "4.5"`
  - 修复：
    - `schema.sql` 与新建迁移 `migrations/add_review_scores_decimal.sql` 将 reviews 表评分字段统一改为 `numeric(2,1)`
    - 小程序 `review.wxml/wxss/js` 重写星星渲染（clip-path 半星）和会员制度选择（“是/否”两个按钮，默认“是”）
    - H5 `Review.tsx` 会员制度默认改为“是”
  - 验证：`npm run build` 通过

- **评价系统 Bug #1 修复：H5 Profile 已评价按钮跳转错误**
  - 问题：`src/pages/customer/Profile.tsx` 中「已评价，查看评价」跳转店铺详情页，无法查看评价内容
  - 修复：跳转路径从 `/customer/shop/${shopId}` 改为 `/customer/review/${booking.id}`
  - 验证：本地构建通过 `npm run build`，无 TS 类型错误

- **修复 schema.sql 注释、小程序星星渲染及发型师显示问题**
  - 问题：`schema.sql` 开头 `#` 注释导致 Supabase SQL Editor 报错；小程序评价页星星仍不可见；预约详情发型师显示“待安排”
  - 修复：
    - `schema.sql` 开头注释改为 SQL 标准 `--`
    - 小程序评价页所有星星改用原生 `<text>★</text>` 字符
    - `api/routes/index.ts` 的 `bookingFromDb` 增加 `barberId`/`barberName` 兼容字段
  - 验证：`npm run build` 通过

- **评价系统小程序端完整补齐**
  - 新增 `mini-program/api/review.js` 与 H5 接口对齐（getReviewByBookingId / getCustomerReviews / createReview）
  - 新增 `mini-program/pages/review/review` 四件套，支持评分/标签/匿名/评论/提交成功全套流程
  - `mini-program/app.json` 注册评价页路由
  - Profile 页 loadProfile 时加载顾客评价构建 bookingId→review 映射，详情弹窗+历史列表均有评价入口
  - `app.wxss` 补充 `.btn-success`、`.booking-link*` 绿色与蓝色评价入口样式
  - 三端评分项命名、半星精度、完成态校验、1:1 bookingId 关联逻辑完全对齐

- **取消/退款系统完成（详见上方章节）**
  - 后端取消、整单退款、订单项级部分退款三接口
  - H5 管理后台取消/退款弹窗 + 订单项级部分退款表单
  - H5 / 小程序顾客端取消/申请退款入口 + 退款历史展示
  - 储值支付原路返还；支付通道支付写入 refund_records 待线下处理

### 2026-08-01

- **更新小程序 AppID**
  - 将 `mini-program/project.config.json` 中的 `appid` 从 `touristappid` 替换为真实小程序 AppID `wxe9a36d7a5c55c325`。
  - 状态：已完成，可在微信开发者工具中重新编译/上传。

- **修复网页端店铺设置保存 500**
  - 问题：`PUT /api/shops/shop1` 因 `shops` 表缺少 `stockholder_config` 等字段返回 500。
  - 修复：新建 `migrations/add_shops_missing_columns.sql` 补充缺失字段，并同步更新 `schema.sql`。

- **修复小程序订单页 WXSS 编译错误**
  - 问题：`product-orders.wxss` 中残留 SCSS 嵌套语法 `&:active`、`&:last-child`，导致 WXSS 编译失败、页面白屏。
  - 修复：将嵌套选择器展开为独立 WXSS 选择器，并全量扫描 `mini-program/**/*.wxss` 确保无残留。

- **修复小程序首页/商品页请求超时**
  - 问题：`GET /api/shops/shop1` 响应体约 2.6MB，其中 `products[0].images[0]` 为约 2.5MB 的 base64 大图，导致网络传输 10~58 秒、小程序超时白屏。
  - 修复：后端接口返回前截断 base64 图片；小程序请求超时延长至 30 秒；首页增加错误提示细化与重试按钮。

- **小程序首页顶部导航栏按 H5 风格改造**
  - 问题：原小程序首页使用蓝色渐变头部+圆形图标按钮，与 H5 风格不一致。
  - 修复：
    - 顶部改为白色导航栏，左侧显示店铺名，右侧放置"个人中心"蓝色按钮和"退出登录"红色描边按钮（均带图标+文字）。
    - 清空小程序原生导航栏标题 `navigationBarTitleText`，避免顶部出现"MBS 理发店"。
    - 底部保留"商品商城""立即预约"两个 H5 风格按钮。
  - 涉及文件：`mini-program/pages/index/index.wxml`、`mini-program/app.wxss`、`mini-program/app.json`。
  - 验证：需重新编译小程序后在开发者工具及真机预览。

- **小程序首页按钮文字居中（暂缓深挖）**
  - 问题：首页"个人中心""商品商城""立即预约"等按钮文字在部分设备/基础库下视觉未严格居中。
  - 状态：已尝试多版 flex/inline-block/line-height 方案，因小程序基础库渲染差异未完全解决。当前 UI 功能完整，文字偏移幅度较小，暂不作为阻塞项，后续统一处理组件库时根治。

- **新增小程序储值充值页面**
  - 问题：购物车余额不足时点击"去储值页面充值"，因小程序无 `recharge` 页面而跳转失败返回个人中心。
  - 修复：
    - 后端新增 `POST /api/customers/:id/recharge` 顾客自助储值接口（不依赖员工权限）。
    - 小程序新增 `pages/recharge/recharge`，支持选择 500/1000/2000/5000 档位充值，展示当前余额和折扣。
    - 页面已注册到 `app.json`。
  - 涉及文件：`api/routes/index.ts`、`mini-program/api/customer.js`、`mini-program/pages/recharge/*`、`mini-program/app.json`。

- **新增待支付商品订单继续支付功能**
  - 问题：商品订单创建后状态为 pending，点击"去支付"提示"支付功能开发中"。
  - 修复：
    - 后端新增 `POST /api/product-orders/:id/pay`，支持余额支付和到店自提付款。
    - 小程序订单列表页 `handlePay` 改为弹窗选择支付方式，余额不足时引导充值或切换到店自提。
  - 涉及文件：`api/routes/index.ts`、`mini-program/api/product.js`、`mini-program/pages/product-orders/product-orders.js`。

**项目外部进展**
  - 小程序已申请完成，AppID 已获取。
  - 开放平台、域名申请审批工作正在进行中。

### 2026-07-28

- **修复 Vercel 构建类型错误**
  - 问题：`api/routes/index.ts` 存在错误导入 `from './app'`，且 `api/app.ts` 挂载类型不匹配。
  - 修复：统一路由入口，补全 `.js` 后缀。
  - 提交：`b1ab752`

- **修复 Vercel ESM 运行时模块找不到**
  - 问题：动态 `import('./app')` 缺少 `.js` 后缀，Vercel Node ESM 运行时找不到模块。
  - 修复：`import('./app.js')`。
  - 提交：`b822bcd`

- **补充所有 shared/ 相对导入 .js 后缀**
  - 问题：`shared/lib/membership.ts`、`shared/mockData.ts` 等 extensionless import 在 Vercel 运行时失败。
  - 修复：批量补全 `.js`。
  - 提交：`38c66c2`

- **暴露 bookings 接口真实错误信息**
  - 问题：`/api/bookings` 返回 500 但错误信息被吞。
  - 修复：在错误响应中返回 `details` 和 `stack`。
  - 提交：最新一次 push（待补充 commit hash）

- **定位环境变量污染问题**
  - 问题：`SUPABASE_SERVICE_ROLE_KEY` 值末尾被带入换行符和 `VITE_USE_REAL_API=true`，导致 `Headers.set` 抛错。
  - 状态：已定位，待用户在 Vercel 后台清理环境变量并重新部署。

---

## 当前阻塞问题

无活跃阻塞问题。

### #1 `SUPABASE_SERVICE_ROLE_KEY` 环境变量被污染（已解决）

- **状态**：✅ 已确认正常
- **历史现象**：`/api/bookings?shopId=shop1&dateStart=...` 曾返回 500
- **历史错误**：
  ```
  TypeError: Headers.set: "<SUPABASE_SERVICE_ROLE_KEY 值>\nVITE_USE_REAL_API=true"
  is an invalid header value.
  ```
- **根因**：Vercel 后台 `SUPABASE_SERVICE_ROLE_KEY` 的值里曾混入换行符和下一行环境变量赋值。
- **修复动作**：已在 Vercel 后台清理环境变量，重新定义后未再修改。
- **验证**：最近推送未再出现该问题，`/api/bookings` 等接口正常。

---

## 环境信息

| 项目 | 值 |
|------|-----|
| 生产域名 | `https://www.hsxx8888.cn`（`www.hfmbs.cn` 仍保留，待小程序审核通过后清理） |
| Vercel 项目 | `mbs` |
| 数据库 | Supabase |
| 关键环境变量 | `SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`、`VITE_USE_REAL_API` |
| Node 版本 | 20/22 |
| 构建命令 | `npm run build`（`tsc -b && vite build`） |
| 后端入口 | `api/index.ts` → 动态 `import('./app.js')` → Express `api/app.ts` |

---

## 已验证的端点

| 端点 | 状态 | 备注 |
|------|------|------|
| `GET /api/health` | ✅ | 返回 `{"success":true}` |
| `GET /api/bookings?shopId=shop1&dateStart=...` | ✅ | 正常返回预约数据 |
| `GET /api/customers` | ⚠️ | 401 未登录，预期行为 |
| `GET /api/settlements?shopId=shop1` | ⚠️ | 401 未登录，预期行为 |
| `POST /api/settlements`（微信支付） | ✅ | 真实 Native 支付已通过 0.01 元实测 |
| `POST /api/webhook/wechat-pay` | ✅ | 回调验签解密成功，自动更新结算状态 |
| `GET /api/shops/shop1` | ✅ | 响应体已降至 KB 级 |
| `POST /api/upload/image` | ⚠️ | 接口返回 200，但返回的 Supabase URL 在国内无法访问 |
| `POST /api/analytics/events/batch` | ✅ | 埋点接口正常，数据写入 Supabase `customer_events` |

---

## 待办 / 下一步

### 立即要做（24 小时内）

1. **等待小程序 1.0.0 审核结果**
   - 登录微信公众平台 → 版本管理 → 查看"审核版本"状态
   - 若通过：发布线上版本，并从微信后台删除 `www.hfmbs.cn`
   - 若不通过：按驳回原因修改后重新上传 v1.0.1

2. **图片存储迁移方案决策**
   - 确认是否使用阿里云 OSS / 腾讯云 COS
   - 提供云存储 AccessKey、Bucket、地域信息

### 本周内完成

3. **图片存储迁移实施**
   - 修改 `server/routes/upload.ts` 接入国内对象存储
   - 测试上传、访问、小程序/H5 图片显示

4. **完成三端测试清单**
   - **评价系统**：H5 顾客端、小程序顾客端、管理后台三端数据一致。
   - **商品订单**：发货 / 确认收货 / 取消 / 退款 / 物流查询三端互通。
   - **储值充值**：小程序/H5/后端余额一致。

5. **小程序真机验证**
   - 首页、商品列表、商品详情正常加载
   - 预约流程端到端正常
   - 图片显示正常（迁移后）

### 等待外部流程

6. **小程序审核与发布**
   - 状态：⏳ 审核中
   - 审核通过后正式发布

### 已知暂缓项

7. **小程序首页按钮文字居中**
   - 状态：已记录为已知项，功能完整，偏移幅度较小
   - 待后续统一处理组件库时根治

---

## 系统未完成任务清单

| 模块 | 未完成任务 | 优先级 | 阻塞项 | 备注 |
|------|-----------|--------|--------|------|
| 结算系统 | 余额支付、权益抵扣场景验证 | 高 | 无 | 现金支付已通，需继续验证 |
| 结算系统 | 微信支付真实 Native 支付回调域名切换 | 高 | 域名备案 | 备案通过后切到 `www.hfmbs.cn` |
| 结算系统 | 组合支付场景验证 | 中 | 无 | 余额 + 权益 + 现金 |
| 结算系统 | 微信支付退款、对账等进阶能力 | 低 | 无 | 当前仅完成收款，退款/对账单待后续 |
| 数据库 | 执行 `migrations/create_payments_table.sql` 创建 payments 表 | 高 | 无 | 微信支付前置，若线上未执行需补齐 |
| 部署 | Vercel 重新部署并验证线上 | 高 | 无 | 本次修改需 push 后自动部署 |
| 图表 | 老板视图 Recharts warning 线上验证 | 中 | Vercel 部署 | 代码已修复 |
| 小程序 | 评价页、首页、预约详情真机验证 | 高 | 备案 / 域名 | 开发者工具可先验证 |
| 小程序 | 全量 mbs-icon 使用点审查 | 低 | 无 | 关键按钮加文字兜底 |
| 三端协同 | 评价系统三端数据一致 | 高 | 无 | H5/小程序/管理后台 |
| 三端协同 | 商品订单发货/收货/取消/退款三端一致 | 高 | 无 | 后端已完成，前端待验证 |
| 三端协同 | 储值充值三端一致 | 中 | 无 | 小程序/H5/后端 |
| 性能 | `/api/shops/shop1` 响应体 KB 级验证 | ✅ | Vercel 部署 | 已验证响应体降至 KB 级 |
| 图片存储 | Supabase Storage 迁移至国内对象存储 | 高 | 待用户决策云厂商 | 上传成功但国内无法访问 |
| 外部 | 域名备案 | ✅ | 已备案通过 | `www.hsxx8888.cn` 已可用 |
| 外部 | 小程序审核/发布 | 高 | 微信审核 | 已提交 v1.0.0，等待结果 |
| 技术债务 | stylist/barber 命名统一 | 低 | 无 | 已兼容，长期废弃 barber |
| 技术债务 | 顾客端 JWT 认证统一 | 低 | 小程序资质 | 用户决定暂缓 |
| 技术债务 | 预约状态变更操作日志 | 低 | 无 | 可选审计需求 |

---

## 风险与注意事项

- `.env` 文件不提交 Git，新机器部署时必须手动同步环境变量。
- Vercel 环境变量编辑时容易把多行文本粘贴进一个值框，导致污染。粘贴后必须检查值末尾是否干净。
- 后端 API 改动必须 push 并等 Vercel 部署完成后，小程序/H5 才能访问到最新接口。
- 任何环境变量修改后必须重新部署，否则不会生效。
