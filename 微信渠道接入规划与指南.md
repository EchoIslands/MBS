# 微信渠道接入规划与指南

> 本文档以理论系统框架形式，集中记录 MBS 项目在微信公众号 H5、微信小程序、网页端三端打通过程中的目标、原理、路径、流程、踩坑和经验。任何与微信生态接入相关的问题、方案、教训都应补充到这里，避免散落在各次会话中。

---

## 第1章 目标与范围

### 1.1 总体目标

让顾客无论从哪个入口进入 MBS，看到的都是同一个账户、同一份订单、同一张会员卡：

- **网页端**：现有 H5 网页，顾客通过浏览器访问。
- **公众号 H5**：店铺方申请认证服务号 + 已备案域名，将现有 H5 移植到公众号内使用。
- **微信小程序**：后续开发小程序版本，与 H5/网页端共用同一套后端。

所有数据打通，包括：客户数据、预约数据、订单/结算数据、支付数据、会员/积分数据、员工数据。

### 1.2 适用场景

本规划与指南适用于以下情形：

- MBS 项目需要从单一网页端扩展到微信生态多入口；
- 需要实现顾客在网页端、公众号 H5、小程序之间身份一致、数据同步；
- 需要接入微信支付 JSAPI（公众号 H5）和/或小程序支付；
- 需要以理发店营业执照为主体完成微信资质申请、商户号绑定、开放平台认证等技术准备工作。

### 1.3 术语表

| 术语 | 说明 |
|------|------|
| MBS | 本项目美发店会员与预约管理系统 |
| 网页端/H5 | 基于 React + Vite 构建的顾客端 Web 应用，通过浏览器访问 |
| 公众号 H5 | 将 H5 页面部署到已认证服务号内，通过微信浏览器访问 |
| 小程序 | 微信小程序原生应用，目录 `/mini-program` |
| 服务号 | 微信公众号的一种类型，认证后可获得网页授权和模板消息权限 |
| 订阅号 | 微信公众号的另一种类型，无网页授权和模板消息权限 |
| 商户号 | 微信支付商户平台账号，用于接收和管理交易资金 |
| 开放平台 | 微信开放平台（`open.weixin.qq.com`），用于跨应用身份打通 |
| AppID | 公众号/小程序在微信平台的唯一标识 |
| mch_id | 微信支付商户号唯一标识 |
| openid | 用户在某个公众号或小程序内的唯一标识 |
| unionid | 同一主体下多个应用绑定到同一开放平台后，用户的统一标识 |
| JSAPI | 公众号 H5 内调起微信支付的接口方式 |
| 小程序支付 | 小程序内通过 `wx.requestPayment` 调起微信支付 |
| ICP 备案 | 网站域名在中国大陆运营前必须完成的工信部备案 |

---

## 第2章 微信生态理论基础

### 2.1 四要素模型

微信生态接入的核心可以归纳为四个要素：公众号、小程序、商户号、开放平台。它们分别承担不同职责，缺一不可。

| 要素 | 职责 | 典型能力 | 在 MBS 中的作用 |
|------|------|---------|----------------|
| 公众号（服务号） | 企业与用户的连接触点 | 网页授权、模板消息、菜单配置 | H5 入口、消息触达 |
| 小程序 | 微信原生轻应用 | `wx.login`、扫码、订阅消息、小程序支付 | 交易闭环主入口 |
| 商户号 | 资金收付主体 | 微信支付、分账、转账 | 统一收款与结算 |
| 开放平台 | 跨应用身份枢纽 | 应用绑定、unionid 下发 | 三端用户身份打通 |

### 2.2 身份体系：openid 与 unionid

#### 2.2.1 openid

- 每个用户在每个公众号或小程序中都有独立的 openid。
- 同一用户在不同公众号、不同小程序中的 openid 完全不同。
- openid 用于单应用内的登录、支付、消息推送。

#### 2.2.2 unionid

- 当公众号和小程序绑定到同一个微信开放平台账号，并且属于同一主体时，微信才会返回 unionid。
- unionid 是同一用户在不同应用之间的统一身份标识。
- unionid 是三端数据打通的基础。

#### 2.2.3 MBS 中的身份存储

在 `customers` 表中新增字段：

```sql
alter table customers add column if not exists openid_oa text;
alter table customers add column if not exists openid_mini text;
alter table customers add column if not exists unionid text;
alter table customers add column if not exists wechat_avatar text;
alter table customers add column if not exists wechat_nickname text;
```

**关键点：**

- `unionid` 是跨端身份打通的核心；
- `openid_mini` 用于小程序支付和推送；
- `openid_oa` 用于公众号模板消息（如果后续做）。

### 2.3 支付体系：JSAPI 与小程序支付

#### 2.3.1 公众号 H5 支付（JSAPI）

- 用户在公众号内打开 H5 页面，点击支付按钮。
- 后端调用微信统一下单接口，返回调起参数。
- 前端调用 `WeixinJSBridge.invoke('getBrandWCPayRequest', ...)` 调起支付。
- 必须配置 JS 接口安全域名和 H5 支付域名，支付域名需审核 3-5 个工作日。

#### 2.3.2 小程序支付

- 用户在小程序内点击支付按钮。
- 后端同样调用微信统一下单接口，返回小程序专用调起参数。
- 前端调用 `wx.requestPayment(...)` 调起支付。
- 不需要 H5 支付域名审核，但需要小程序 AppID 与商户号绑定。

#### 2.3.3 统一支付服务抽象

建议后端新增 `paymentService`，统一处理：

```ts
// 创建支付订单（渠道无关）
createPaymentOrder({ bookingId, settlementId, channel: 'wechat_mini' | 'wechat_h5', amount })

// 微信统一下单（根据渠道返回不同调起参数）
unifiedOrder({ paymentId, channel, openid })

// 支付回调（统一更新订单状态）
handleWechatCallback(xmlData)
```

这样小程序和 H5 共用同一套订单和回调逻辑，只有前端调起方式不同。

### 2.4 数据打通原理

#### 2.4.1 为什么必须绑定开放平台？

公众号和小程序各自有独立的 openid，同一用户在不同端的 openid 完全不同。只有将两个应用绑定到同一个微信开放平台账号下，才能获取统一的 unionid，这是三端数据打通的基础。

#### 2.4.2 数据打通后的典型场景

1. **跨端登录**：用户在公众号注册后，小程序用同一微信登录，无需重复注册。
2. **跨端支付**：H5 支付、公众号支付、小程序支付都走同一个商户号，资金统一结算。
3. **消息推送**：从小程序触发，向用户在公众号侧发送模板消息（如预约提醒）。

#### 2.4.3 顾客身份合并策略

必须提前确定：小程序用户进来后，如何与现有顾客记录关联？

**推荐方案：手机号优先匹配**

1. 小程序端 `wx.login()` 拿到 code；
2. 后端换取 `openid` 和 `unionid`；
3. 小程序端引导用户授权手机号（`getPhoneNumber`）；
4. 后端用手机号在 `customers` 表中查找：
   - 找到：把 `openid_mini` 和 `unionid` 更新到该记录；
   - 未找到：用手机号新建顾客记录，并写入 `openid_mini` 和 `unionid`。

**备选方案：unionid 优先匹配**

- 如果 H5 已经通过公众号网页授权拿到了 `unionid`，可以直接用 `unionid` 匹配。
- 但当前 H5 还没接入公众号授权，所以**手机号优先更现实**。

---

## 第3章 接入路径决策

### 3.1 完整三端打通路径

1. **先做 H5**：当前网页端继续迭代，保持可用。
2. **店铺申请域名并备案**：以理发店营业执照为主体购买域名，完成 ICP 备案。
3. **认证微信服务号**：以理发店主体申请并认证服务号（不是订阅号）。
4. **将 H5 移植到店铺域名下**：最终用店铺的网站承载公众号 H5。
5. **再做小程序**：服务号 H5 跑通后，开发小程序版本，与 H5 共用后端和数据。

### 3.2 小程序优先简化路径

如果希望前期尽快跑通支付、降低公众号 H5 支付的复杂度，可以选择**只在小程序内完成支付**，H5/网页端暂时只做预约展示、信息浏览等不涉及资金流转的功能。

#### 3.2.1 策略说明

- **H5/网页端**：保留现有功能（预约、排队、查看套餐、会员信息、评价等），但**不接入微信支付**。如需付费，页面引导用户“去小程序完成支付”。
- **小程序**：作为**唯一支付入口**，顾客在小程序内登录、预约、结算、支付。
- **数据打通**：H5 和小程序仍然共用同一套后端和数据库，用户身份通过 unionid 关联，预约订单、会员积分、储值余额等数据完全一致。

#### 3.2.2 适用场景

- 想先验证小程序闭环，避免一次性投入公众号认证、H5 支付域名审核等较长周期。
- H5 主要作为信息展示/引流入口，真正的交易闭环放在小程序。
- 店铺暂时不想立刻做 ICP 备案或认证服务号。

#### 3.2.3 该路径下的流程变化

| 原路径步骤 | 变化说明 |
|-----------|---------|
| 步骤 1：域名备案 | **可延后**。H5 不做支付时，若 H5 不挂在公众号内，可暂时不备案域名；但若要接入公众号菜单，仍需备案域名。 |
| 步骤 2：微信支付商户号 | **仍然需要**。小程序支付必须绑定商户号。 |
| 步骤 3：微信开放平台 | **仍然需要**。H5 与小程序要打通 unionid，必须绑定同一开放平台。 |
| 步骤 4：认证服务号 | **可延后/可选**。如果 H5 只是普通网页访问，不需要服务号；如果需要公众号菜单入口、模板消息，仍需认证服务号。 |
| 步骤 5-10：公众号 H5 支付相关 | **全部跳过**。不做公众号 H5 支付，不需要 JSAPI、网页授权支付域名等配置。 |
| 步骤 11-17：小程序 | **仍然需要**。小程序认证、商户号绑定、开放平台绑定、服务器域名、登录、支付全部要完成。 |
| 步骤 18-19：数据打通 | **仍然需要**。通过 unionid 把 H5 用户和小程序用户关联起来。 |
| 步骤 20-21：公众号模板消息 | **可延后/可选**。如果暂时不依赖公众号触达用户，可以不做；小程序本身也可以发订阅消息。 |
| 步骤 22：域名切换与 H5 支付配置 | **可大幅简化**。只需保证 H5 域名能正常访问，不需要配置 H5 支付域名。 |

#### 3.2.4 该路径的优缺点

**优点：**

- 前期认证、备案、审核环节更少，能更快上线支付闭环。
- 小程序支付体验更稳定，微信原生支持更好。
- H5 可以先作为宣传/预约入口，不承担支付合规压力。

**缺点：**

- 顾客在 H5 看到价格后必须跳转到小程序支付，路径多一步，转化率可能略低。
- 若后续要让 H5 也支持支付，仍需补齐公众号认证、域名备案、H5 支付域名审核等步骤。
- 如果不用服务号，H5 无法获取微信用户信息（unionid 可能为空），跨端身份关联会弱一些。

### 3.3 决策树

```
是否需要 H5 直接完成支付？
    │
    ├── 是 → 公众号 H5 支付必须做
    │         ├── 需要：认证服务号 + ICP 备案域名 + H5 支付域名审核
    │         └── 适合：营销活动多、朋友圈传播强、老用户习惯公众号支付
    │
    └── 否 → 小程序优先支付
              ├── 仍然需要：小程序、商户号、开放平台
              ├── 可延后：服务号认证、域名备案（若不入公众号菜单）
              └── 适合：想快速跑通交易闭环、资源有限、到店服务型业务
```

### 3.4 MBS 当前选择

**MBS 当前阶段选择：小程序优先支付，公众号做宣传和预约入口。**

即使选择“只在小程序支付”，也建议按以下顺序推进，避免后期返工：

1. **先申请微信开放平台并完成认证**（数据打通的核心）。
2. **申请微信支付商户号**（小程序支付必需）。
3. **开发小程序并完成支付闭环**（预约 → 结算 → 支付 → 查看订单）。
4. **H5 保留现有功能，增加“去小程序支付”引导**。
5. **后续根据业务需要，再决定是否补齐公众号 H5 支付**。

---

## 第4章 业主端资质申请操作路径

> 2026-07-24 更新：因小程序申请、账户开通、API 审批等事宜现由业主/店铺端直接负责，本章将这些行政类工作从技术-行政混合清单中独立出来，形成一份只面向业主的操作路径表，便于按表推进、逐项交接。

### 4.1 核心原则

- 所有微信生态账号必须以**理发店营业执照主体**申请，确保主体一致，否则后续绑定会被拒绝。
- 商户号、小程序、开放平台三者绑定后，才能实现支付、登录、unionid 打通。
- 开发端需要的只是账号、AppID、证书、权限开通结果；具体申请过程由业主完成。

### 4.2 业主端资质申请路径总表

| 阶段 | 序号 | 事项 | 申请入口/平台 | 所需材料 | 预计周期 | 产出物 | 关键注意点 | 完成后交接给开发端 |
|---|---|---|---|---|---|---|---|---|
| 准备 | 1 | 确认营业执照可用 | - | 营业执照照片、法人身份证正反面、对公银行账户信息 | 1 天内 | 完整资质材料包 | 个体工商户或企业均可；经营范围最好包含美发/生活服务 | 营业执照类型、法人姓名 |
| 小程序 | 2 | 注册微信小程序 | mp.weixin.qq.com | 营业执照、管理员身份证、手机号、邮箱 | 即时 | 小程序 AppID | 选择"企业"主体；账号类型选"小程序" | 小程序 AppID、管理员账号密码 |
| 小程序 | 3 | 小程序微信认证 | 小程序后台 → 微信认证 | 营业执照、对公账户、认证公函（需盖章） | 1-3 个工作日 | 认证成功通知 | 认证费 300 元/年；必须认证才能支付、获取手机号 | 认证完成截图 |
| 支付 | 4 | 申请微信支付商户号 | pay.weixin.qq.com | 营业执照、法人身份证、对公账户、门店照片/经营场所证明 | 1-7 个工作日 | 商户号 mch_id | 与小程序同一主体；审核可能要求补充材料 | 商户号、登录账号、超级管理员信息 |
| 平台 | 5 | 注册微信开放平台 | open.weixin.qq.com | 营业执照、法人身份证、邮箱 | 即时 | 开放平台账号 | 建议用小程序/公众号同一主体注册 | 开放平台账号、密码 |
| 平台 | 6 | 开放平台开发者资质认证 | 开放平台后台 → 资质认证 | 营业执照、对公账户、认证公函 | 1-3 个工作日 | 认证成功通知 | 未认证无法绑定小程序/公众号获取 unionid | 认证完成截图 |
| 绑定 | 7 | 商户号绑定小程序 AppID | 商户平台 → 产品中心 → APPID 授权管理 | 小程序 AppID | 即时（主体一致） | 绑定成功 | 小程序后台需确认授权 | 绑定完成截图 |
| 绑定 | 8 | 小程序绑定开放平台 | 开放平台 → 管理中心 → 公众账号/小程序 | 小程序 AppID、管理员扫码确认 | 即时 | 绑定成功 | 必须与未来可能申请的公众号绑定到同一开放平台 | 绑定完成截图 |
| 域名 | 9 | 购买域名并完成 ICP 备案 | 域名服务商 + 工信部备案系统 | 营业执照、法人身份证、域名证书、核验单 | 1-20 个工作日 | 已备案域名 | 备案主体必须与商户号主体一致；域名用于 H5/小程序后台配置 | 域名、备案号 |
| 域名 | 10 | 配置 SSL 证书 | 域名服务商 / 证书服务商 / Vercel 自带 | 已备案域名 | 1 天内 | HTTPS 可访问域名 | 小程序只支持 HTTPS；必须配置有效证书 | 域名 HTTPS 验证通过 |
| 配置 | 11 | 配置小程序服务器域名 | 小程序后台 → 开发 → 开发设置 → 服务器域名 | 已备案 HTTPS 域名 | 即时 | 域名配置成功 | request 合法域名、uploadFile、downloadFile 按需配置 | 配置完成的域名列表 |
| 权限 | 12 | 申请"商家转账到零钱" | 商户平台 → 产品中心 → 商家转账到零钱 | 商户号、经营场景说明、转账用途说明 | 1-7 个工作日 | 权限开通通知 | 用于股东提现到微信零钱；需说明真实业务场景 | 权限开通截图、可用额度 |
| 证书 | 13 | 下载微信支付 API 证书 | 商户平台 → 账户中心 → API 安全 | 商户号管理员权限 | 即时 | apiclient_cert.pem、apiclient_key.pem | 证书是敏感文件，切勿上传 GitHub；只交给可信开发人员 | 证书文件（安全传输） |
| 权限 | 14 | 添加开发者为小程序成员 | 小程序后台 → 成员管理 | 开发者微信号 | 即时 | 开发者权限开通 | 至少给"开发权限"和"体验权限" | 开发者微信收到邀请确认 |
| 发布 | 15 | 提交小程序代码审核并发布 | 小程序后台 → 版本管理 | 小程序版本号、更新说明 | 1-3 个工作日 | 审核通过/发布成功 | 审核前需完成服务器域名、合法域名配置 | 线上版本号 |

### 4.3 申请顺序与依赖关系

目标：**在合法合规的前提下，最大化并行推进，把行政审批的等待时间重叠起来，避免任何一个环节因为"误以为有依赖"而被延后启动。**

#### 4.3.1 核心结论（先读这段）

1. **所有资质申请只有一个共同前置条件：营业执照及相关法人材料齐全。** 只要这一步完成，下面四条主线可以**完全并行**启动，互不等待。
2. **真正需要等待的行政审批只有 3 类**：
   - ICP 备案（1–20 个工作日，最长）
   - 微信支付商户号审核（1–7 个工作日）
   - 微信认证/开放平台资质认证（1–3 个工作日）
3. **大量看似有依赖的事项，其实是"配置动作"**，等前置资质一下来，几小时内就能完成，不该被当成阻塞点。
4. **总耗时 = 最长那条行政审批路径的耗时 + 最后几个配置动作的耗时**。理论上最快 **2 周左右**可以全部跑通，常规情况 **2–4 周**。

#### 4.3.2 真阻塞点 vs 伪依赖

| 类型 | 事项 | 为什么必须等 | 等待对象 |
|---|---|---|---|
| **真阻塞点** | ICP 备案 | 域名购买后才能提交；工信部/接入商审核不可控 | 营业执照 + 域名购买完成 |
| **真阻塞点** | 微信支付商户号审核 | 腾讯风控/资质审核 | 营业执照 + 法人材料 |
| **真阻塞点** | 小程序微信认证 | 微信支付 300 元认证费 + 腾讯审核 | 小程序注册完成 |
| **真阻塞点** | 开放平台开发者资质认证 | 腾讯审核 | 开放平台注册完成 |
| **真阻塞点** | 商家转账到零钱审批 | 微信支付评估经营场景 | 商户号审核通过 |
| **伪依赖** | 域名购买 | 很多人以为要等小程序注册，其实只要有营业执照就可以买 | 无需等待任何微信账号 |
| **伪依赖** | ICP 备案提交 | 不需要等小程序、商户号、开放平台；备案系统只看主体资质 | 无需等待任何微信账号 |
| **伪依赖** | 申请商户号 | 不需要等小程序注册；两者可以同步填资料 | 只需营业执照 |
| **伪依赖** | 注册开放平台 | 不需要等小程序或商户号；反而是开放平台要先准备好，才能后续绑小程序 | 只需营业执照 |
| **伪依赖** | 配置 SSL 证书 | 域名解析后即可配置，不依赖备案通过（但生产环境必须等备案） | 域名解析完成 |

**关键认知转变**：不要把"申请 → 审核 → 通过"串成一条线，再把几条线串成更长的线。正确的做法是：**审核类工作全部并行发起，配置类工作等结果出来后瞬间补齐。**

#### 4.3.3 依赖关系总览

| 主线 | 事项 | 内部依赖 | 与外部主线的交叉依赖 | 关键说明 |
|---|---|---|---|---|
| **A. 小程序与认证** | 注册小程序 → 小程序认证 | 注册完成后才能认证 | 认证完成后才能：绑定商户号、绑定开放平台、配置服务器域名 | 注册是秒级的；认证需要 1–3 个工作日 |
| **B. 商户号与证书** | 申请商户号 → 下载 API 证书 → 申请商家转账到零钱 | 商户号通过后才能下载证书、申请转账权限 | 商户号通过后，才能与小程序做 AppID 绑定 | 申请商户号可与 A/C/D 同步启动 |
| **C. 开放平台** | 注册开放平台 → 开放平台资质认证 | 注册完成后才能认证 | 认证完成后才能绑定小程序 | 绑定后才下发 unionid，是数据打通的关键 |
| **D. 域名与备案** | 购买域名 → ICP 备案 → 配置 SSL 证书 → 配置小程序服务器域名 | 域名购买后才能备案；备案通过后才能配置 SSL 和服务器域名 | 配置服务器域名前需要 A 完成（小程序后台操作） | **这条线与 A/B/C 几乎没有交叉，应最早启动** |

#### 4.3.4 时间最优流程（带阶段标注）

```
阶段 0：准备材料（1 天内完成）
───────────────────────────────
[营业执照照片 / 法人身份证 / 对公账户 / 手机号 / 邮箱]
              │
              ▼
阶段 1：四线并行启动（第 0–1 天，全部主动提交）
───────────────────────────────
    ┌─────────┼─────────┬─────────┐
    │         │         │         │
    ▼         ▼         ▼         ▼
  注册      申请      注册      购买
  小程序   商户号    开放平台    域名
    │         │         │         │
    ▼         │         │         ▼
  小程序      │       开放平台    ICP
  认证      （等）     资质认证    备案
    │         │         │         │
    │         │         │         ▼
    │         │         │      配置 SSL
    │         │         │       证书
    │         │         │         │
    ▼         ▼         ▼         ▼
阶段 2：交叉绑定（审核通过当天完成，约 1–2 小时）
───────────────────────────────
  商户号    小程序    小程序     配置
  绑定     绑定开放  服务器域名  完成
  小程序    平台
    │         │         │         │
    └─────────┴─────────┴─────────┘
              │
              ▼
阶段 3：开发权限与可选能力（1–3 天内完成）
───────────────────────────────
  添加开发者  →  申请商家转账到零钱（可选）  →  下载 API 证书
              │
              ▼
阶段 4：开发与发布（与阶段 1–3 可重叠）
───────────────────────────────
  本地/预览开发  →  提交代码审核  →  发布上线
```

#### 4.3.5 关键依赖的精确说明

下面把容易被误解的依赖关系逐条拆开：

**（1）域名 / ICP 备案：几乎是最独立的线**

- 只需要：**营业执照 + 法人身份证 + 域名购买完成**。
- 不依赖：小程序 AppID、商户号、开放平台、公众号。
- 可以早到什么程度：**营业执照还没拿到时，就可以先询价、选域名；拿到当天立刻购买并提交备案。**
- 为什么常被耽误：很多人觉得"我还没注册小程序，买了域名也没用"。错了——备案是流程中最长的等待项，必须最早启动。

**（2）商户号申请：与小程序、开放平台完全独立**

- 只需要：**营业执照 + 法人身份证 + 对公账户 + 门店/经营场所材料**。
- 不依赖：小程序注册、开放平台注册、域名备案。
- 审核期间可以同时做：注册小程序、注册开放平台、购买域名、提交备案。

**（3）小程序注册与认证：只依赖自己**

- 注册：秒级。
- 认证：需要注册完成后才能发起，审核 1–3 个工作日。
- 认证通过后，才能被商户号绑定、被开放平台绑定、配置服务器域名。
- 但**注册这个动作本身不依赖任何其他账号**，拿到营业执照当天就应完成。

**（4）开放平台注册与认证：反而应该尽早**

- 开放平台是后续"小程序绑定开放平台"的前置。
- 注册秒级，认证 1–3 个工作日。
- 不依赖小程序、商户号、域名。
- 常见错误：等小程序跑通了才想起开放平台，导致 unionid 打通被拖延。

**（5）绑定动作：都是配置，不是行政审批**

- 商户号绑定小程序：需要小程序 AppID + 商户号，主体一致时即时生效。
- 小程序绑定开放平台：需要小程序 AppID + 开放平台账号，主体一致时即时生效。
- 配置服务器域名：需要已备案域名 + SSL + 小程序 AppID，后台即时生效。
- 这三件事的共同特点是：**只要前置资质下来了，1 小时内就能全部做完。**

**（6）商家转账到零钱：支付闭环的可选项**

- 前置：商户号审核通过。
- 用途：股东微信提现自动打款。
- 如果不做它：小程序支付、退款、会员充值依然可以跑通；股东提现只能走"抵扣消费"或人工线下处理。

#### 4.3.6 按天/周推进清单（建议版）

| 时间 | 业主端动作 | 开发端动作 | 产出物 | 阻塞风险 |
|---|---|---|---|---|
| **Day 0** | 同时启动：① 注册小程序；② 申请商户号；③ 注册开放平台；④ 购买域名并提交 ICP 备案 | 准备开发环境、确认 API 域名与接口规划 | 4 个申请提交成功截图/邮件 | 材料不齐会导致全部卡住 |
| **Day 1–3** | 完成小程序认证、开放平台资质认证；配合商户号补充材料 | 同步开发小程序前端页面（可用测试域名或本地开发） | 认证通过通知 | 商户号审核可能要求补材料 |
| **Week 1–2** | 等待 ICP 备案、商户号审核；期间可准备 SSL 证书 | 继续开发，重点跑通预约/会员/展示流程 | 备案通过通知 / 商户号通过通知 | ICP 备案是最大不确定性 |
| **审核通过当天** | ① 商户号绑定小程序；② 小程序绑定开放平台；③ 配置服务器域名；④ 添加开发者为成员 | 将真实域名配置进小程序、更新 API 基地址 | 绑定截图 / 服务器域名配置截图 | 主体不一致会导致绑定失败 |
| **Week 2–3** | 下载 API 证书并安全交接；可选申请商家转账到零钱 | 接入微信支付统一下单、回调、退款 | 证书文件 / 支付联调通过 | 证书传输方式要安全 |
| **Week 3–4** | 提交小程序代码审核、发布上线 | 修复审核反馈问题 | 线上版本号 | 审核可能要求补充说明 |

#### 4.3.7 决策点与取舍建议

| 决策 | 选项 A（推荐） | 选项 B | 选择建议 |
|---|---|---|---|
| 是否立刻购买店铺域名并备案？ | 是，拿到营业执照当天就买 | 先用个人域名 / 不备案 | **选 A**。备案是最大瓶颈，越早越好；个人域名做支付有主体不一致风险 |
| 是否立刻注册开放平台？ | 是，与小程序同步 | 等小程序跑通再注册 | **选 A**。开放平台认证也需要时间，且是 unionid 打通前提 |
| 商户号是否自己申请？ | 是，以理发店主体申请 | 先用第三方聚合支付 | **选 A**。自有商户号才能绑定小程序、走官方支付接口 |
| 是否申请商家转账到零钱？ | 同步申请 | 先不做，只支持抵扣消费 | 如果时间允许建议同步申请；若着急上线，可先上线后再补 |
| H5 是否也要接入支付？ | 当前只做小程序支付，H5 只做展示/预约 | H5 也接入公众号支付 | **当前选 A**，可大幅降低域名/服务号/支付域名审核压力 |

### 4.4 业主与开发端交接清单

每完成一步，业主需要把以下信息交给开发端：

| 阶段 | 交接内容 | 交接方式建议 |
|---|---|---|
| 小程序注册完成 | 小程序 AppID、AppSecret | 加密文档或私聊 |
| 小程序认证完成 | 认证成功通知截图 | 截图 |
| 商户号申请完成 | 商户号 mch_id、商户名称 | 文档 |
| 开放平台认证完成 | 开放平台账号、绑定关系截图 | 截图 |
| 商户号绑定小程序完成 | 绑定成功截图 | 截图 |
| 小程序绑定开放平台完成 | 绑定成功截图 | 截图 |
| 域名备案完成 | 已备案域名、备案号 | 文档 |
| SSL 证书配置完成 | 域名 HTTPS 可访问验证 | 链接 |
| 服务器域名配置完成 | 已配置的域名列表截图 | 截图 |
| 商家转账到零钱开通 | 权限开通截图、可用额度 | 截图 |
| API 证书下载完成 | apiclient_cert.pem、apiclient_key.pem | 安全传输（不要发微信/邮件） |
| 添加开发者完成 | 开发者微信号确认已接受 | 口头/消息确认 |

### 4.5 常见阻塞点与应对

| 阻塞点 | 原因 | 应对 |
|---|---|---|
| 商户号审核不通过 | 经营范围不匹配或材料不全 | 提前确认营业执照包含"美发服务""生活美容服务"等 |
| 小程序认证公函盖章麻烦 | 个体户无公章 | 可用法人签字 + 手指印替代，具体看微信要求 |
| 域名备案主体与商户号不一致 | 备案用了个人或其他公司 | 必须重新以理发店主体备案 |
| 商家转账到零钱被拒绝 | 场景说明不充分 | 准备股东奖励/合伙人分润的业务说明材料 |
| 开放平台绑定失败 | 小程序已绑定其他开放平台 | 一个应用只能绑定一个开放平台，需先解绑 |
| API 证书泄露 | 证书文件被不当传输 | 使用加密压缩包或安全通道，禁止上传到 GitHub/Vercel |

---

## 第5章 开发端技术接入流程

### 5.1 阶段零：核心前置准备

| 序号 | 工作事项 | 关键操作 | 前置条件 | 注意事项 |
|------|---------|---------|---------|----------|
| 1 | 理发店购买新域名并完成 ICP 备案 | 以理发店营业执照为主体购买域名，提交企业备案 | 营业执照、法人身份证 | 备案审核 1-20 个工作日；支付域名必须已备案且主体与商户号一致 |
| 2 | 申请微信支付商户号 | 以理发店营业执照为主体申请 | 营业执照、法人身份证、对公账户 | 个体工商户和企业均可；后续所有支付功能的前提 |
| 3 | 申请并认证微信开放平台账号 | 访问 `open.weixin.qq.com` 注册并完成开发者资质认证 | 营业执照、法人身份证 | 数据打通的核心枢纽；认证审核约 1-3 个工作日 |

### 5.2 阶段一：公众号开发与集成

| 序号 | 工作事项 | 关键操作 | 前置条件 | 注意事项 |
|------|---------|---------|---------|----------|
| 4 | 申请已认证的微信服务号 | 以理发店主体申请服务号，完成微信认证 | 营业执照 | 只有认证服务号才有网页授权和模板消息权限 |
| 5 | 商户号与公众号 AppID 绑定 | 商户平台 → 产品中心 → APPID 授权管理 → 发起绑定 → 公众号后台确认 | 步骤 2、4 完成 | 主体一致时无需审核；一个商户号最多绑定 50 个 AppID |
| 6 | 公众号配置服务器信息 | 获取 AppID 和 AppSecret，配置 IP 白名单、网页授权域名、JS 接口安全域名 | 步骤 5 完成 | 网页授权域名必须已 ICP 备案 |
| 7 | 公众号与开放平台绑定 | 微信开放平台 → 绑定关系 → 将公众号绑定到开放平台账号 | 步骤 3、4 完成 | 同主体一个开放平台最多绑定 50 个公众号 |
| 8 | 实现公众号 H5 登录 | 网页授权：用户点击 → 获取 code → 后端换取 openid 和 unionid | 步骤 6、7 完成 | 静默授权 `snsapi_base` 只能拿到 openid |
| 9 | 接入公众号 H5 支付（JSAPI） | 后端调用统一下单，前端调用 `WeixinJSBridge.invoke` 发起支付 | 步骤 5 完成，支付域名已配置 | 支付回调域名主体需一致；H5 支付域名配置需审核 3-5 个工作日 |
| 10 | 数据库存储公众号身份标识 | 在用户表存储 `openid_oa` 和 `unionid` | 步骤 8 完成 | 后续数据打通的关键字段 |

### 5.3 阶段二：小程序开发与集成

| 序号 | 工作事项 | 关键操作 | 前置条件 | 注意事项 |
|------|---------|---------|---------|----------|
| 11 | 申请微信小程序并完成认证 | 以理发店主体注册小程序，完成微信认证 | 营业执照 | 认证约 1-3 个工作日 |
| 12 | 商户号与小程序 AppID 绑定 | 商户平台发起授权绑定，小程序后台确认 | 步骤 2、11 完成 | 与公众号绑定同一商户号 |
| 13 | 小程序绑定到同一开放平台账号 | 微信开放平台 → 绑定关系 → 小程序 | 步骤 3、11 完成 | 必须与公众号绑定到同一个开放平台，才能获得相同 unionid |
| 14 | 小程序配置服务器域名 | 小程序后台 → 开发设置 → 配置 request 合法域名 | 步骤 13 完成 | 与 H5 共用同一套后端 API |
| 15 | 实现小程序登录 | 调用 `wx.login()` 获取 code → 后端 `jscode2session` 换取 openid 和 unionid | 步骤 13、14 完成 | 绑定开放平台后，接口才会返回 unionid |
| 16 | 小程序支付集成 | 调用 `wx.requestPayment` 发起支付 | 步骤 12 完成 | 流程与公众号 H5 类似，使用小程序专用接口 |
| 17 | 数据库存储小程序身份标识 | 在用户表存储 `openid_mini` 和 `unionid` | 步骤 15 完成 | 与步骤 10 的数据在同一张表 |

### 5.4 阶段三：数据全面打通

| 序号 | 工作事项 | 关键操作 | 前置条件 | 注意事项 |
|------|---------|---------|---------|----------|
| 18 | 建立用户身份映射机制 | 通过 unionid 将同一个用户的 openid_oa 和 openid_mini 关联 | 步骤 3、7、13 完成 | 只有绑定到同一开放平台，unionid 才能关联 |
| 19 | 实现跨端业务数据同步 | 预约、订单、会员、储值卡数据以 unionid 为索引，三端共享 | 步骤 18 完成 | 这是“数据全通”的核心实现层 |
| 20 | 配置公众号模板消息 | 公众号后台 → 模板消息 → 添加模板 → 记录 template_id | 步骤 6 完成 | 用于从小程序触发，向用户推送公众号消息 |
| 21 | 实现“小程序触发 → 公众号推送” | 服务端通过 unionid 查出 openid_oa，调用公众号发送模板消息 | 步骤 10、17、20 完成 | 用户必须已关注公众号；需提前在小程序后台关联公众号 |

### 5.5 阶段四：系统迁移与全面联调

| 序号 | 工作事项 | 关键操作 | 前置条件 | 注意事项 |
|------|---------|---------|---------|----------|
| 22 | 域名切换与支付配置 | 将系统迁移到理发店已备案的新域名；商户平台配置新域名为 H5 支付域名 | 步骤 1 完成 | 新域名配置 H5 支付需审核 3-5 个工作日 |
| 23 | 三端全链路测试 | 测试三端登录、支付、预约、会员信息完全一致 | 全部前置工作完成 | 验证数据打通是否成功 |

### 5.6 关键提醒

1. **域名备案主体必须与商户号主体一致**。不一致需提供授权函，且仅限同集团、品牌与经销商、SaaS 服务三种关系。
2. **一个公众号/小程序只能绑定一个开放平台账号**，绑定前确认账号正确。
3. **公众号必须是“已认证的服务号”**，订阅号无网页授权和模板消息权限。
4. **商户号绑定 AppID 后暂不支持解绑**，绑定前确认信息无误。
5. **用户必须关注公众号才能收到模板消息**，需在引导流程中设计关注环节。

---

## 第6章 技术准备与架构

> 小程序不是独立 App，必须与现有 H5/网页端共用同一套后端和数据。本章分析在写小程序代码之前，应该完成哪些前期准备，才能保证最终三端数据相通、体验一致。

### 6.1 核心原则

- **后端一套，多端共用**：小程序、H5、网页端调用同一组 `/api` 接口。
- **数据一份，统一索引**：顾客、预约、订单、支付等数据都存在同一个 Supabase 数据库。
- **身份统一，登录态隔离**：不同端用不同的登录方式获取 token，但识别的是同一个顾客。

### 6.2 数据层准备

#### 6.2.1 统一类型定义

- 检查 `/shared/types.ts` 是否完整覆盖：
  - 顾客（Customer）
  - 预约（Booking）
  - 结算/订单（Settlement / Order）
  - 支付（Payment）
  - 服务/商品（Service / Product）
  - 员工（Employee）
  - 店铺（Shop）
- 小程序端直接复用这些类型，避免两端各自定义导致字段不一致。

#### 6.2.2 数据库字段扩展

至少需要为顾客表增加微信生态字段：

```sql
alter table customers add column if not exists openid_oa text;
alter table customers add column if not exists openid_mini text;
alter table customers add column if not exists unionid text;
alter table customers add column if not exists wechat_avatar text;
alter table customers add column if not exists wechat_nickname text;
```

**关键点：**

- `unionid` 是跨端身份打通的核心；
- `openid_mini` 用于小程序支付和推送；
- `openid_oa` 用于公众号模板消息（如果后续做）。

#### 6.2.3 顾客身份合并策略

同第 2.4.3 节，推荐**手机号优先匹配**方案。

### 6.3 API 层准备

#### 6.3.1 接口返回格式统一

当前后端接口返回格式：

```json
{ "success": true, "data": { ... } }
```

小程序端直接复用这套格式，不要另起一套。

#### 6.3.2 鉴权体系梳理

当前系统里有几类用户：

| 用户类型 | 当前鉴权方式 | 是否支持小程序 |
|----------|-------------|---------------|
| 员工 | JWT token（`authMiddleware`） | 否 |
| H5 顾客 | `customerId` 存在本地 storage | 否，不安全 |
| 小程序顾客 | 需要新增 | 待开发 |

**必须新增：顾客端统一鉴权**

建议引入自定义顾客 token（也可以用 JWT）：

```
POST /api/auth/customer/login
请求：{ phone, code? }  // H5 用手机号+验证码，小程序用 code
响应：{ success: true, data: { token, customer } }
```

然后新增一个 `customerAuthMiddleware`，校验顾客 token：

```ts
// 保护顾客端敏感接口
/customer/profile
/customer/bookings
/customer/payments
```

**关键点：**

- 员工接口和顾客接口要分开保护；
- 小程序登录和 H5 手机号登录最终都返回同一个顾客 token；
- 支付、查看订单等接口用顾客 token 保护，不要再传 `customerId` 明文。

#### 6.3.3 小程序专用接口

需要新增（或改造）以下接口：

| 接口 | 说明 |
|------|------|
| `POST /api/auth/mini-login` | 小程序 code 换登录态 |
| `POST /api/auth/mini-phone` | 小程序授权手机号 |
| `POST /api/payments/mini-prepay` | 小程序支付统一下单 |
| `PUT /api/bookings/:id/cancel` | 顾客取消预约（已在 3.15.6 修复） |

### 6.4 认证体系设计

#### 6.4.1 推荐方案：双 token 体系

保留现有员工 JWT，新增顾客 JWT：

- 员工 token payload：`{ id, shopId, role, name, phone }`
- 顾客 token payload：`{ customerId, phone, shopId }`

前端存储：

- H5：`localStorage.setItem('customer_token', token)`
- 小程序：`wx.setStorageSync('customer_token', token)`

接口调用：

- 员工接口：`Authorization: Bearer <employee_token>`
- 顾客接口：`Authorization: Bearer <customer_token>`

#### 6.4.2 为什么不能用 `customerId` 明文传参？

当前 H5 很多接口直接传 `customerId`，比如：

```ts
bookingApi.getMyBookings(currentCustomer.id)
```

这在小程序里也能跑，但存在安全问题：用户可以改 `customerId` 查看别人的数据。所以必须改成 token 鉴权。

**改造范围：**

- 所有 `/api/customer/*` 接口；
- 所有涉及顾客隐私的查询（预约、订单、余额、积分）。

### 6.5 支付层准备

#### 6.5.1 当前 H5 支付现状

需要先确认当前 H5 支付是怎么实现的：

- 是否已经是微信支付 JSAPI？
- 支付回调接口在哪里？
- 订单/结算记录如何生成？

#### 6.5.2 抽象支付服务

同第 2.3.3 节，建议后端新增 `paymentService`。

#### 6.5.3 支付表设计

如果还没有独立的支付记录表，建议新增：

```sql
create table if not exists payments (
  id text primary key,
  customer_id text references customers(id),
  booking_id text references bookings(id),
  settlement_id text references settlements(id),
  channel text, -- wechat_mini / wechat_h5 / cash / balance
  amount numeric,
  status text default 'pending', -- pending / paid / failed / refunded
  transaction_id text, -- 微信支付订单号
  paid_at timestamptz,
  created_at timestamptz default now()
);
```

### 6.6 前端架构选择

#### 6.6.1 方案 A：小程序独立开发（推荐当前阶段）

- H5 继续用 React + Vite；
- 小程序用原生或 Taro/React 开发；
- 两端独立维护，但共用后端 API 和类型定义。

**优点：**

- 技术栈清晰，互不干扰；
- 小程序可以完全按微信规范优化体验；
- H5 改动风险小。

**缺点：**

- 部分 UI 逻辑需要写两遍。

#### 6.6.2 方案 B：跨端框架（Taro / Uni-app）

- 一套代码同时输出 H5 和小程序。

**优点：**

- 减少重复开发。

**缺点：**

- 改造现有 H5 成本大；
- 框架限制多，微信支付、地图等原生能力适配复杂；
- 对当前项目来说，ROI 不高。

**建议：当前用方案 A，业务稳定后再评估是否迁移跨端框架。**

### 6.7 域名与网络准备

#### 6.7.1 后端 API 域名要求

- 必须支持 HTTPS；
- 必须已完成 ICP 备案；
- 域名需要在小程序后台配置为 request 合法域名。

#### 6.7.2 图片/静态资源域名

如果小程序需要显示店铺图片、发型师头像，需要配置：

- `downloadFile` 合法域名；
- 图片资源最好放在支持 HTTPS 的 CDN 或对象存储。

#### 6.7.3 开发调试

- 小程序开发者工具支持不校验域名（开发版）；
- 真机预览/体验版必须配置合法域名；
- 如果后端本地开发没有 HTTPS，可用内网穿透工具（如 ngrok）。

### 6.8 开发顺序建议

不要一上来就写小程序页面，按这个顺序：

1. **第 1 步：后端鉴权改造**
   - 新增顾客 token 机制；
   - 把 H5 顾客接口从 `customerId` 明文改为 token 鉴权。

2. **第 2 步：数据库字段扩展**
   - `customers` 表增加 `openid_mini`、`unionid` 等。

3. **第 3 步：后端小程序登录接口**
   - 实现 `POST /api/auth/mini-login`。

4. **第 4 步：小程序框架搭建 + 登录页**
   - 先跑通登录，拿到顾客 token。

5. **第 5 步：支付接口准备**
   - 抽象支付服务，实现小程序 prepay 接口。

6. **第 6 步：小程序业务页面**
   - 首页、预约、结算、支付、订单。

7. **第 7 步：H5 增加去小程序支付引导**
   - 确保即使 H5 不做支付，用户也能平滑跳转到小程序。

### 6.9 小程序与 H5 独立开发时如何保证数据与逻辑同步

> 小程序和 H5 前端代码各自维护，但数据必须同源、核心逻辑必须复用。本节给出具体可执行的措施。

#### 6.9.1 数据同源（已具备）

- 网页端与 H5 共用同一套 React + Vite 代码，天然同步；
- 小程序单独开发前端，但调用同一组 `/api` 接口和同一个 Supabase 数据库；
- 顾客、预约、订单、积分、余额等核心数据只有一份，不存在多副本同步问题。

#### 6.9.2 复用层必须抽到 `shared/`

| 复用内容 | 当前位置 | 目标位置 | 说明 |
|----------|----------|----------|------|
| 实体类型 | `shared/types.ts` | 保持 | Customer、Booking、Settlement、Payment 等 |
| API 请求封装 | `src/api.ts` | `shared/api.ts` | 不依赖浏览器 DOM 的请求逻辑、接口函数签名 |
| 业务工具函数 | `src/lib/` | `shared/lib/` | 会员折扣、积分计算、排队等待时间等纯计算逻辑 |
| Mock 数据 | `shared/mockData.ts` | 保持 | 开发期兜底数据 |

**操作步骤（已完成）：**

1. ✅ 在 `shared/` 下新建 `api-base.ts`，将通用 `http` 封装、API 基础地址获取、真实 API 开关迁移过来；
2. ✅ 在 `shared/lib/` 下新建 `membership.ts` 和 `avatar.ts`，将会员折扣、积分、VIP 有效期、头像 URL 生成等纯函数迁移过来；
3. ✅ `src/api.ts` 改为从 `shared/api-base.ts` 复用 `http`、`getApiBase()`、`isRealApi()`；
4. ✅ `src/lib/membership.ts`、`src/lib/avatar.ts` 改为从 `shared/lib/` 重新导出，保持 H5 现有 import 路径不变。

#### 6.9.3 流程与命名一致

- 核心操作流程两端必须一致：预约 → 确认 → 结算 → 支付 → 评价；
- 状态文案统一：待确认、已确认、已完成、已取消；
- 页面结构统一：首页、预约、排队、个人中心；
- 同一功能在不同端使用相同名称，避免用户混淆。

**建议维护一份"H5/小程序页面映射表"：**

| H5 页面 | 小程序页面 | 说明 |
|---------|-----------|------|
| `/customer/home` | `pages/index/index` | 首页 |
| `/customer/booking/:shopId` | `pages/booking/index` | 预约 |
| `/customer/queue/:id` | `pages/queue/index` | 排队 |
| `/customer/profile` | `pages/profile/index` | 个人中心 |
| `/customer/checkout` | `pages/checkout/index` | 结算 |

#### 6.9.4 允许差异的地方

- **支付调起**：H5 用 `WeixinJSBridge.invoke`，小程序用 `wx.requestPayment`；
- **登录方式**：H5 用手机号，小程序用 `wx.login` + 手机号授权；
- **原生能力**：小程序可调扫码、订阅消息；H5 需走公众号能力。

这些差异应在架构上隔离：后端逻辑统一，前端只在"最后一公里"按平台适配。

### 6.10 小程序框架搭建后的下一步操作步骤

> 小程序基础框架 `/mini-program` 已搭建完成。在店铺端小程序 AppID、商户号、开放平台等资质到位前，仍有大量技术工作可以并行推进。本节把"下一步"变成可直接执行的操作步骤。

#### 6.10.1 当前可立即推进的工作（不依赖微信资质）

| 工作项 | 依赖 | 优先级 | 说明 |
|--------|------|--------|------|
| 小程序首页接入真实店铺/服务数据 | 后端 `/api/shops/*` 接口 | P0 | 验证共享层是否工作，让小程序不再是空壳 |
| 小程序预约页复用 H5 预约逻辑 | `shared/lib/membership.ts`、后端预约接口 | P0 | 保证两端预约体验和数据一致 |
| H5 增加"去小程序支付"引导 | 无 | P1 | 提前让用户习惯跳转小程序 |
| 后端支付接口骨架（prepay / callback） | `payments` 表 | P1 | 资质到位后只需填入微信统一下单逻辑 |
| 后端顾客端 token 鉴权改造 | 无 | P1 | 为小程序和 H5 共用接口做准备（需用户确认是否现在做） |

#### 6.10.2 推荐下一步：让小程序首页和预约页接入真实数据

**理由：**

1. **不依赖微信资质**：不需要 AppID、商户号，直接复用现有后端接口；
2. **验证多平台同步**：能立刻检验 `shared/api-base.ts`、`shared/types.ts` 在小程序端是否可用；
3. **小程序从"空壳"变成"可用预览"**：店铺端可以先用微信开发者工具看到效果；
4. **为后续登录和支付打好基础**：页面数据流跑通后，加登录和支付只是增量改造。

#### 6.10.3 具体操作步骤

**步骤 1：创建小程序端 API 模块**

在 `mini-program/api/` 下创建：

- `shop.js`：获取店铺信息、服务列表、发型师列表；
- `booking.js`：创建预约、获取预约详情；
- `customer.js`：获取当前顾客信息（先用 `customerId` 明文，后续统一改 token）。

这些模块内部调用 `mini-program/utils/api.js` 的 `get/post`，API 基础地址自动来自 `shared/api-base.ts`。

**步骤 2：小程序首页接入店铺数据**

修改 `pages/index/index.js`：

1. `onLoad` 时调用 `shopApi.getShop('shop1')`；
2. 把店铺名称、地址、电话、营业时间渲染到首页；
3. 展示店铺服务列表入口。

**同步要求：**

- 首页展示的数据字段必须与 H5 首页一致；
- 服务列表的数据结构必须直接复用 `shared/types.ts` 中的 `Service` 类型。

**步骤 3：小程序预约页复用 H5 预约逻辑**

修改 `pages/booking/booking.js`：

1. 加载默认店铺的服务列表和发型师列表；
2. 生成未来 7 天日期和可用时段；
3. 实现"随机发型师"自动匹配逻辑，**必须与 H5 的 `Booking.tsx` 算法一致**；
4. 点击"确认预约"时调用 `bookingApi.createBooking`，参数结构与 H5 保持一致。

**同步要求：**

- 预约页的数据结构、字段命名、状态值必须与 H5 相同；
- 随机发型师匹配算法必须复用 `shared/lib/` 中的逻辑（如果 H5 匹配逻辑还没抽到 shared，本次一并抽象）。

**步骤 4：H5 与小程序预约流程对齐检查**

对比检查两端以下流程是否一致：

- 选择服务 → 选择日期/时间 → 选择/匹配发型师 → 确认预约 → 跳转排队页；
- 预约成功后跳转的页面路径：H5 是 `/customer/queue/:id`，小程序是 `/pages/queue/queue?id=...`；
- 预约失败时的提示文案和兜底行为。

**步骤 5：本地验证**

1. `npm run build` 通过（确保 H5 端没被破坏）；
2. 微信开发者工具打开 `/mini-program`；
3. 首页能正确显示店铺信息；
4. 预约页能正确显示服务、发型师、日期时段；
5. 创建预约后，小程序和 H5 看到的预约数据一致（同一数据库）。

#### 6.10.4 验证标准

- [x] 小程序首页展示真实店铺信息，与 H5 首页字段一致；
- [x] 小程序预约页展示真实服务列表和发型师列表；
- [x] 小程序创建预约后，数据进入同一数据库，H5 端能查到该预约；
- [x] `npm run build` 通过；
- [x] 微信开发者工具无报错。

> 2026-07-20 更新：以上验证项已全部通过。当前小程序可通过 `www.hfmbs.cn/api` 与 H5/后端实现数据同步。
>
> 2026-07-21 更新：在数据同步基础上，已完成小程序 UI 与 H5 统一改造，详见第 7 章 7.2 节 UI/交互改造记录与宪章 2.5.4《小程序 UI 统一规范》。

#### 6.10.5 小程序 UI 统一改造规范

> 小程序与 H5 顾客端面向同一批用户，视觉与交互必须一致。本节把宪章 2.5.4 的原则落到小程序开发的具体文件和规范。

**设计 Token**

- 唯一来源：`mini-program/app.scss` 中的 CSS 变量。
- 关键变量与 H5 对应关系：

| 小程序变量 | 取值 | H5 对应 |
|---|---|---|
| `--primary` | `#2563eb` | `blue-600` |
| `--accent` | `#f97316` | `orange-500` |
| `--success` | `#22c55e` | `green-500` |
| `--danger` | `#ef4444` | `red-500` |
| `--background` | `#f9fafb` | `gray-50` |
| `--text-primary` | `#1f2937` | `gray-800` |
| `--text-secondary` | `#6b7280` | `gray-500` |

- 扩展变量时，必须先在 `app.scss` 定义，并同步确认/新增 H5 端对应色值。

**通用组件**

- **图标**：统一使用 `components/icon/icon`，页面引用名为 `mbs-icon`。所有页面 `.json` 必须在 `usingComponents` 中注册 `"mbs-icon": "/components/icon/icon"`。
- **按钮**：统一使用 `app.scss` 提供的 `.btn`、`.btn-primary`、`.btn-secondary`、`.btn-primary-plain`、`.btn-small`。
- **卡片**：统一使用 `.card`、`.card-title`、`.card-subtitle`。
- **标签/徽章**：统一使用 `.tag-*`、`.badge-*`、`.status-*`。
- **空状态**：统一使用 `.empty-state`、`.empty-icon`、`.empty-text`。
- **底部固定栏**：统一使用 `.fixed-bottom-bar`。
- **登录弹窗**：统一使用 `components/login-modal/login-modal`。

**页面结构模板**

每个顾客端页面推荐按以下顺序组织 WXML：

1. `.gradient-header-*` 顶部渐变头部（含页面标题与副标题）；
2. `.container` 内容区，内部用 `.card` 堆叠；
3. 需要时底部 `.fixed-bottom-bar`；
4. 全局一次引用的 `<login-modal />`。

**新增/调整 UI 的检查清单**

- [ ] 是否能在 H5 找到对应页面/组件？若结构不同，需说明原因。
- [ ] 颜色是否来自 `app.scss` 变量？是否同步 H5 颜色？
- [ ] 图标是否已在 `components/icon/icon.wxml` 中定义？名称是否与 H5 一致？
- [ ] 状态/文案是否与 H5 相同？是否复用 `shared/lib/membership.ts` 等共享层？
- [ ] 登录触发是否统一使用 `login-modal`？
- [ ] 是否两端同时回归关键页面？

#### 6.10.6 资质到位后再做的事

以下工作必须等店铺端拿到资质后才能继续：

| 工作项 | 所需资质 |
|--------|---------|
| 配置小程序 request 合法域名 | 小程序 AppID + 后端 HTTPS 域名 |
| 实现 `wx.login` 登录 | 小程序 AppID |
| 实现 `wx.requestPayment` 支付 | 小程序 AppID + 商户号 mch_id |
| 获取 unionid | 小程序绑定到微信开放平台 |
| H5 手机号与小程序用户关联 | 小程序手机号授权 + 后端顾客表 |

#### 6.10.7 测试阶段技术调整

> 为简化微信开发者工具配置、避免 TypeScript 编译问题，当前 `/mini-program` 目录已统一使用 `.js` 文件运行。该调整仅影响测试阶段源码格式，不影响后期正式版功能；小程序仍与 H5/网页端共用同一套后端 API 和数据库，三方协同逻辑不变。

本地验证时请注意：

1. **导入目录**：在微信开发者工具中选择「导入项目」，路径指向仓库根目录下的 `mini-program` 子目录（不是整个仓库根目录）。
2. **关闭域名校验**：开发版小程序默认会校验 request 合法域名，测试环境请在「详情 → 本地设置」中勾选「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」，以允许请求未备案的测试域名。
3. **模拟登录态**：当前顾客端尚未接入 `wx.login`，请在微信开发者工具控制台执行：
   ```js
   wx.setStorageSync('mbs_customer_id', '真实顾客ID');
   ```
   之后重新加载首页/预约页，即可用该顾客身份调用后端接口。
4. **确认文件后缀**：`app.json` 中页面路径无需写后缀，但对应文件必须存在 `.js`（如 `pages/index/index.js`）。若仍残留 `.ts` 文件，需删除避免开发者工具混淆。
5. **API 响应解包**：后端统一返回 `{ success: true, data: ... }`，小程序端需在 `mini-program/api/*.js` 中解包 `result.data`，不能直接返回 `get/post` 的原始结果。
6. **避免使用 H5 特有 API**：
   - ❌ `URLSearchParams` → 改用手动 `encodeURIComponent` 拼接 query string；
   - ❌ `localStorage` → 改用 `wx.setStorageSync` / `wx.getStorageSync`；
   - ❌ `fetch` → 改用 `wx.request`；
   - ❌ WXML 中不支持 `.find()`、箭头函数、`?.` 可选链 → 在 `.js` 中预计算后绑定。
7. **网络超时**：Vercel Serverless 冷启动可能较慢，建议把 `wx.request` 超时设为 30 秒。
8. **使用生产域名测试**：Vercel 自动分配的 `*.vercel.app` 域名在国内可能访问超时，测试时直接使用已备案的生产域名（如 `www.hfmbs.cn/api`）。

---

## 第7章 问题与解决方案库

> 本章将实际推进中遇到的问题和解决方案，按问题类型重新组织，保留所有原始细节，便于按类型检索和复用。

### 7.1 Runtime 兼容问题

#### 7.1.1 `requestAnimationFrame is not a function`

- **发现时间**：2026-07-24
- **现象**：微信开发者工具真机调试进入预约页 `pages/booking/booking`，Console 报红：`TypeError: requestAnimationFrame is not a function`，同时可能伴随 `Cannot read properties of null (reading 'bg')` 和 `_global is not defined` 等连锁错误。
- **根因**：`booking.js` 的 `onServiceScroll` 滚动监听中使用了浏览器 API `requestAnimationFrame` 做节流，但微信小程序 Runtime 不提供该函数。
- **修复**：将 `requestAnimationFrame(() => { ... })` 替换为 `setTimeout(() => { ... }, 16)`，并在进入异步前提前解构 `e.detail`，避免事件对象被回收。
- **相关文件**：`mini-program/pages/booking/booking.js`。
- **教训**：小程序不是浏览器，任何浏览器专属 API（`requestAnimationFrame`、`URLSearchParams`、`localStorage`、`fetch`、DOM API 等）都必须先用 `wx.*` 或 `setTimeout` 替代；新增交互前应先在真机/开发者工具 Console 确认无报错。

#### 7.1.2 `URLSearchParams` 不可用

- **发现时间**：2026-07-20
- **现象**：小程序调用 `getNearbyShops`、`getBookingsByShop` 等接口时，构建 query string 失败。
- **根因**：微信小程序 Runtime 不支持 `URLSearchParams`。
- **修复**：替换为手动 query string 拼接。
- **相关文件**：`mini-program/api/shop.js`、`mini-program/api/booking.js`。
- **教训**：见 7.1.1 教训。

#### 7.1.3 WXML 表达式不支持 JavaScript 方法调用

- **发现时间**：2026-07-21
- **现象**：排队页、个人中心、结算页、首页的 WXML 中直接调用页面 JS 方法（如 `{{distance.toFixed(1)}}`、`{{formatDateTime(...)}}`、`{{getPurchaseVIPLabel(...)}}` 等），微信小程序 WXML 表达式不支持调用页面方法，会导致渲染异常或空白。
- **修复**：
  - 新增 `mini-program/utils/format.wxs` 通用格式化模块，集中提供日期、短日期、日期时间、折扣、会员/储值标签、权益图标名/色等纯函数；
  - 在 `queue.wxml`、`profile.wxml`、`checkout.wxml`、`index.wxml` 顶部引入 `<wxs src="../../utils/format.wxs" module="fmt" />`；
  - 将上述页面中所有直接调用页面方法的表达式改为 `{{fmt.xxx(...)}}`；
  - 排队页 `distance.toFixed(1)` 改为 `distanceStr`，在 `queue.js` 的 `refreshComputed` 中预计算，避免 WXML 做方法调用；
  - 完成替换后全量扫描 WXML，确保无剩余 `.find()` / `.filter()` / 箭头函数 / 可选链 / 页面方法调用。
- **相关文件**：`mini-program/utils/format.wxs`、`mini-program/pages/queue/queue.wxml`、`mini-program/pages/profile/profile.wxml`、`mini-program/pages/checkout/checkout.wxml`、`mini-program/pages/index/index.wxml`。

#### 7.1.4 WXML 不支持 `<span>` 标签和带小数点的类名

- **发现时间**：2026-07-21
- **现象**：`pages/queue/queue.wxml` 混用 `<span>` 标签和含点的 `gap-1.5` 类，微信小程序不支持。
- **修复**：将 `<span>` 改为 `<text>`，`gap-1.5` 改为 `gap-1_5` 并在 `app.scss` 定义。
- **相关文件**：`mini-program/pages/queue/queue.wxml`、`mini-program/app.scss`。

#### 7.1.5 `wxss` 组件内不能使用标签选择器

- **发现时间**：2026-07-21
- **现象**：`components/icon/icon.scss` 使用 `.mbs-icon svg` 标签选择器，触发微信小程序组件 wxss 限制警告。
- **修复**：改为 `.mbs-icon-svg` 类选择器，并给所有 `<svg>` 节点补 `class="mbs-icon-svg"`。
- **相关文件**：`mini-program/components/icon/icon.scss`、`mini-program/components/icon/icon.wxml`。

#### 7.1.6 `switchTab` 不支持 URL 参数

- **发现时间**：2026-07-20
- **现象**：tabBar 页面间跳转无法传参。
- **根因**：小程序 `switchTab` 不支持 URL 参数。
- **修复**：新增 `utils/storage.js` 中的 `setRouteParams` / `takeRouteParams` 工具，用本地缓存中转参数；`pages/index/index.js`、`pages/booking/booking.js`、`pages/queue/queue.js`、`pages/profile/profile.js`、`pages/checkout/checkout.js` 均已适配，确保首页选择服务、预约成功、个人中心点击预约、结算页查看排队等场景能正确携带 `bookingId` / `serviceId`。
- **相关文件**：`mini-program/utils/storage.js` 及各页面 JS。

### 7.2 部署与网络问题

#### 7.2.1 Vercel 自动 preview 域名国内超时

- **发现时间**：2026-07-20
- **现象**：Vercel 自动分配的 `*.vercel.app` preview 域名在国内网络下可能访问超时，小程序/浏览器测试不稳定。
- **解决**：小程序/浏览器测试应直接使用已备案的生产域名（如 `www.hfmbs.cn`）。

#### 7.2.2 后端改动未部署导致接口异常

- **发现时间**：2026-07-20 / 2026-07-21
- **现象**：Trae 修改了 `api/` 后端代码后，如果未 push 到 GitHub 触发 Vercel 部署，小程序在生产域名 `www.hfmbs.cn/api` 上访问的仍是旧后端。表现为新接口（如 `/customers/:id/public`）返回 401，或字段缺失。
- **根因**：后端代码虽正确但尚未重新部署到 `www.hfmbs.cn`。
- **解决**：后端改动必须走 `git push → Vercel 部署完成` 才能在小程序端验证；小程序端对 401 做了兼容，会清空本地 `customerId` 并展示登录表单。
- **补充**：2026-07-21 进一步定位到生产环境 `GET /api/customers/:id/public` 仍返回 401（`authMiddleware` 未下放），而仓库代码已将该接口放在 `customersRouter.use(authMiddleware)` 之前。已给小程序端加兜底：登录成功后直接使用 `/customers/login` 返回的顾客信息，不再强依赖 `/customers/:id/public`，同时保留后端部署后走 public 接口的能力。

#### 7.2.3 H5 网页端走 mock 数据

- **发现时间**：2026-07-20
- **现象**：Vercel 部署成功后，H5 网页端仍可能走 mock 数据。因为 `src/api.ts` 通过 `VITE_USE_REAL_API` 环境变量控制是否调用真实 API，未设置时默认回退到 `mockBookings`/`mockCustomers` 等本地缓存。表现为小程序创建的预约/顾客在店铺端 H5 预约管理页面看不到。
- **解决**：在 Vercel 项目 Settings → Environment Variables 里添加 `VITE_USE_REAL_API=true`，然后重新部署；部署后浏览器打开店铺端预约管理页面，Network 中应能看到对 `/api/bookings?shopId=shop1` 的真实请求。

#### 7.2.4 Vercel Serverless 冷启动慢

- **发现时间**：2026-07-20
- **现象**：后端响应超时。
- **解决**：延长 `wx.request` 超时时间为 30 秒。

### 7.3 数据一致性问题

#### 7.3.1 服务列表未按会员身份计算会员价

- **发现时间**：2026-07-21
- **现象**：`loadShop` 与 `loadCustomer` 并行执行导致服务列表可能未按会员身份计算会员价。
- **修复**：`Promise.all` 结束后调用 `refreshServicesWithCustomer` 重新 enrich 服务价格，登录成功后也同步刷新。
- **相关文件**：`mini-program/pages/index/index.js` 等。

#### 7.3.2 `customerId` 误用与数据库外键约束

- **发现时间**：2026-07-20
- **现象**：小程序模拟登录时传入不存在的 `customerId`，触发数据库外键约束错误。
- **教训**：`customerId` 不是手机号，而是 `customers` 表的 `id` 字段；小程序模拟登录时需传入真实存在的 `customerId`。

### 7.4 登录与认证问题

#### 7.4.1 登录成功后仍停留在登录界面

- **发现时间**：2026-07-21
- **现象**：个人中心/结算页登录成功后仍停留在登录界面。
- **根因**：登录弹窗关闭后，页面自动调用 `loadProfile`/`loadCustomer` 加载资料，若接口失败会重新把 `showLogin` 设为 true，导致弹窗/登录占位再次弹出。
- **修复**：已给加载方法增加 `autoShowLogin` 参数，登录成功后的刷新不再自动重新唤起登录弹窗。
- **相关文件**：`mini-program/pages/profile/profile.js`、`mini-program/pages/checkout/checkout.js`、`mini-program/components/login-modal/login-modal.js`。

### 7.5 支付问题

> 当前尚未正式接入微信支付，待资质到位后补充。核心方向参见第 2.3 节和第 6.5 节。

### 7.6 UI 与交互问题

#### 7.6.1 首页标题与单店定位冲突

- **发现时间**：2026-07-21
- **现象**：首页标题出现“附近理发店”字样，与单店定位冲突。
- **修复**：改为 `{{shop.name || '皓诗形象设计'}}`。
- **相关文件**：`mini-program/pages/index/index.wxml`。

#### 7.6.2 服务列表过长导致预约页体验不佳

- **发现时间**：2026-07-21
- **现象**：服务项目为长列表导致预约页过长，与 H5 `Booking.tsx` 的 `VerticalScrollSlider` 不一致。
- **修复**：改为 `scroll-view` 限制最大高度 480rpx，右侧增加自定义橙色滑块（灰色轨道 + 橙色 thumb，滚动时实时同步位置），并提示“滑动查看更多”。
- **相关文件**：`mini-program/pages/booking/booking.wxml`、`mini-program/pages/booking/booking.scss`。

#### 7.6.3 底部 tabBar 与 H5 不一致

- **发现时间**：2026-07-21
- **现象**：底部常驻 tabBar 与 H5 顾客端（无 tabBar，靠页面内按钮导航）不一致。
- **修复**：已删除 `app.json` 的 `tabBar`，并统一将页面跳转从 `switchTab` 改为 `navigateTo` / `redirectTo`。
- **相关文件**：`mini-program/app.json`。

#### 7.6.4 首页底部操作栏对齐 H5

- **发现时间**：2026-07-21
- **现象**：首页底部操作栏与 H5 ShopDetail 不一致。
- **修复**：改为“商品商城（开发中）+ 立即预约”双按钮。
- **相关文件**：`mini-program/pages/index/index.wxml`。

#### 7.6.5 页面内导航缺失

- **发现时间**：2026-07-21
- **现象**：部分页面缺少返回导航。
- **修复**：给预约页、排队页、结算页增加顶部返回栏，个人中心保留“了解更多”返回店铺。
- **相关文件**：各页面 WXML/SCSS。

### 7.7 构建问题

#### 7.7.1 `npm run build` 因缺少依赖失败

- **发现时间**：2026-07-21
- **现象**：`npm run build` 因环境缺少 `node_modules` 先执行 `npm install`。
- **解决**：执行 `npm install` 后再运行 `npm run build`，最终 H5 build 通过。

#### 7.7.2 组件名与类型重名导致 build 失败

- **发现时间**：2026-07-17
- **现象**：`src/pages/customer/Booking.tsx` 组件名与 `Booking` 类型重名导致 `npm run build` 失败。
- **修复**：重命名为 `BookingPage` 后通过 3 次 check 和 3 次 build 验证。
- **相关文件**：`src/pages/customer/Booking.tsx`。

### 7.8 开发流程经验

#### 7.8.1 推进顺序

- **经验**：先把 H5 跑稳，再迁移到公众号域名，最后做小程序。不要三端同时开工，否则认证、备案、支付配置会互相阻塞。

#### 7.8.2 UI 改动必须同步评估三端

- **发现时间**：2026-07-21
- **经验**：颜色、字体、图标、状态徽章、按钮样式等任何视觉调整，都会同时影响 H5、小程序，甚至后端返回字段（如会员权益文案）。后续任何界面调整必须：
  1. 以 H5 顾客端为基准确认设计；
  2. 同步改 `mini-program/app.scss` / `components/icon` / 页面样式；
  3. 同步改 `shared/lib/membership.ts` 等共享层；
  4. 两端同时目测回归关键页面（首页、预约、排队、个人中心、结算）。

---

## 第8章 决策记录

### 8.1 公众号 H5 支付是否必须做？

#### 8.1.1 基本判断

**结论：公众号 H5 支付不是必须的。对 MBS 当前阶段，优先做小程序支付、公众号只做宣传和预约入口，是更节约资源的合理选择。**

#### 8.1.2 公众号 H5 支付的优劣势

**优势：**

| 优势 | 说明 |
|------|------|
| 用户路径短 | 用户在公众号文章/菜单里直接完成支付，不需要跳转小程序 |
| 营销传播强 | 朋友圈文案、公众号推文可直接挂 H5 支付链接，适合做套餐预售/限时活动 |
| 老用户习惯 | 部分顾客更习惯在公众号内完成操作 |
| 模板消息触达 | 服务号可向已关注用户推送预约提醒、服务完成通知等（触达率通常高于小程序订阅消息） |

**劣势：**

| 劣势 | 说明 |
|------|------|
| 资质成本高 | 必须认证服务号（300 元/年）、域名 ICP 备案、H5 支付域名审核（3-5 天） |
| 开发复杂度高 | 需要网页授权获取 openid、配置 JS 接口安全域名、处理 H5 支付专用接口 |
| 风控更严 | H5 支付要求用户主动点击调起，自动触发容易被微信拦截或降权 |
| 与小程序支付技术栈不同 | 一个是 `WeixinJSBridge.invoke`，一个是 `wx.requestPayment`，需要分别对接和测试 |

#### 8.1.3 小程序支付的优劣势

**优势：**

- 体验更原生，微信官方主推，支付流程稳定。
- 开发相对简单，接口统一。
- 不需要 H5 支付域名审核。
- 天然适合作为交易闭环主入口。

**劣势：**

- 需要单独开发小程序界面。
- 用户无法从朋友圈直接打开小程序，传播路径受限（需扫码或聊天窗口进入）。
- 小程序审核需要周期。

#### 8.1.4 几个容易忽略的关键点

**（1）用户身份打通会变弱**

小程序能拿到 `unionid`，公众号 H5 也能拿到 `unionid`（通过服务号网页授权）。但如果公众号只做宣传平台、不挂需要网页授权的 H5 页面，那么 H5 用户如果没有登录手机号，系统就无法识别他是谁。

**应对方案：**

- H5 端强制用手机号登录；
- 小程序端微信授权后也要绑定手机号；
- 通过手机号把 H5 用户和小程序用户关联起来。

**（2）公众号的模板消息能力不应浪费**

服务号的模板消息（预约提醒、服务完成通知、到账通知）触达效果通常好于小程序订阅消息。如果完全放弃服务号能力，这些触达渠道就没了。

**应对方案：**

- 即使不做 H5 支付，也可以先认证服务号；
- 把服务号作为“消息触达+预约入口”，支付时仍跳小程序。

**（3）H5 支付的独特价值在朋友圈传播**

如果未来做“团购套餐”“限时秒杀”等活动，H5 支付链接可以直接发朋友圈，小程序做不到这一点。这是公众号 H5 支付最大的差异化价值。

**（4）公众号 H5 支付的真正成本不在开发**

H5 已经写好的前提下，接入公众号支付的开发工作量其实不大。真正的成本是：

- 服务号认证 300 元/年
- 域名备案 1-20 个工作日
- H5 支付域名审核 3-5 个工作日
- 网页授权、JS 接口配置等资质类工作

#### 8.1.5 对 MBS 的阶段性建议

**第一阶段（当前到上线）：小程序优先**

1. 申请微信开放平台并完成认证（unionid 是数据打通核心）。
2. 申请微信支付商户号（小程序支付必需）。
3. 开发小程序，完成预约 → 结算 → 支付 → 查看订单的完整闭环。
4. H5/网页端保留非支付功能（预约、排队、查看、评价、会员查询）。
5. H5 页面在需要支付的位置增加“去小程序完成支付”的引导。

**第二阶段（业务稳定后）：按需补齐公众号 H5 支付**

当出现以下情况时，再考虑投入公众号 H5 支付：

- H5 跳转小程序那一步导致明显订单流失；
- 需要开展朋友圈营销活动，H5 直接支付更方便；
- 需要服务号模板消息做精准触达，且愿意承担认证和备案成本。

#### 8.1.6 最终结论

- **公众号 H5 支付有价值，但不是 MBS 当前阶段的必选项。**
- **小程序支付优先 + 公众号做宣传/预约入口**，既能尽快跑通交易闭环，又能节约前期资质和开发成本。
- **唯一需要提前规划的是用户身份打通**：即使不做 H5 支付，也要让 H5 用户留下手机号，否则跨端数据无法合并。

---

## 第9章 附录

### 9.1 开发-部署工作流

当前确定的工作流：

1. Trae 远程环境（`/workspace`）作为代码源头；
2. 开发者从 Trae 下载到本地 `E:\MBS`；
3. 本地提交并推送到 GitHub；
4. Vercel 监听 GitHub 自动部署到 `www.hfmbs.cn`；
5. 微信开发者工具导入 `E:\MBS\mini-program` 进行测试。

### 9.2 与当前 MBS 系统的关联

- **后端 API**：三端共用现有的 `/api` 路由。
- **数据库**：在 `customers` 表中新增 `openid_oa`、`openid_mini`、`unionid` 字段，用于识别同一用户在不同端的身份。
- **支付**：当前已有的微信支付逻辑（JSAPI）需要适配公众号 H5 和小程序的调用方式。
- **登录**：当前顾客端基于 phone/customerId 的登录需要扩展为支持微信授权登录，但保留原有登录方式作为兜底。

### 9.3 检查清单

#### 9.3.1 新增/调整 UI 的检查清单

- [ ] 是否能在 H5 找到对应页面/组件？若结构不同，需说明原因。
- [ ] 颜色是否来自 `app.scss` 变量？是否同步 H5 颜色？
- [ ] 图标是否已在 `components/icon/icon.wxml` 中定义？名称是否与 H5 一致？
- [ ] 状态/文案是否与 H5 相同？是否复用 `shared/lib/membership.ts` 等共享层？
- [ ] 登录触发是否统一使用 `login-modal`？
- [ ] 是否两端同时回归关键页面？

#### 9.3.2 小程序真机验证检查清单

- [ ] 首页展示真实店铺信息，与 H5 首页字段一致；
- [ ] 预约页展示真实服务列表和发型师列表；
- [ ] 创建预约后，数据进入同一数据库，H5 端能查到该预约；
- [ ] `npm run build` 通过；
- [ ] 微信开发者工具无报错。

#### 9.3.3 资质申请检查清单

- [ ] 营业执照信息完整可用；
- [ ] 微信小程序注册并完成认证；
- [ ] 微信支付商户号申请完成；
- [ ] 微信开放平台注册并完成认证；
- [ ] 商户号与小程序 AppID 绑定成功；
- [ ] 小程序绑定到同一开放平台；
- [ ] 域名购买并完成 ICP 备案；
- [ ] SSL 证书配置完成；
- [ ] 小程序服务器域名配置完成；
- [ ] 开发者已添加为小程序成员。

### 9.4 引用链接

| 平台/文档 | 链接 |
|----------|------|
| 微信小程序平台 | `mp.weixin.qq.com` |
| 微信支付商户平台 | `pay.weixin.qq.com` |
| 微信开放平台 | `open.weixin.qq.com` |

---

*文档维护：随小程序开发推进持续更新。*
