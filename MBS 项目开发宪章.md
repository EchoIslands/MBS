# MBS 项目开发宪章

> 版本：v4.1 | 生效日期：2026-09-26
> 
> 本文档是 MBS 项目的最高开发规范，也是人类与 AI Agent 的协作协议。所有改动代码、配置、数据库 Schema、部署设置前，均视为已阅读并同意遵守本宪章。

---

## 第一部分：宪章总纲

### 1.1 宗旨

把踩过的坑、验证过的流程、隐性约束和协作规则显性化，使之成为项目知识资产。让任何开发者（包括 AI 助手）接到任务后，能按统一标准完成开发、验证、部署与交接。

### 1.2 适用对象

- 所有参与 MBS 项目的开发者、代码审查者、项目维护者；
- 与 MBS 项目协作的 AI 助手 / Agent；
- 新加入成员及未来接手项目的人员。

### 1.3 核心原则（铁律）

| 编号 | 原则 | 含义 |
|------|------|------|
| P-01 | 改动必验证 | 任何代码改动后必须执行：本地构建 `npm run build` → 本地后端启动 `npm run server:api` → 浏览器/小程序 Network 检查。任何一步不通过，禁止 push。 |
| P-02 | 线上必实测 | 本地正常不等于线上正常。Vercel 部署后必须实测登录、添加数据、查看数据等核心流程，尤其要实测 POST/PUT 请求体是否正确解析、字段是否正确入库。 |
| P-03 | 数据必显式 | 写数据库前必须显式字段映射并过滤非法 key，禁止直接把整个对象 `toSnakeCase()` 后塞入数据库。 |
| P-04 | 需求必落地 | 任何新需求必须先落到任务书/计划书，定义验收标准并评估影响范围后再动手。 |
| P-05 | 问题必记录 | 踩过的坑、临时方案、隐性约束必须补充到本宪章，避免重复踩坑。 |
| P-06 | 生产数据必保护 | 一旦录入真实用户数据，任何部署切换、环境变量修改、域名迁移、数据库变更都必须以"不影响真实用户和数据"为前提。 |
| P-07 | UI 三端一致 | 网页端、H5、微信小程序面向顾客的可视元素必须统一。设计 Token、通用组件以 H5 顾客端为基准，小程序复刻。 |
| P-08 | 代码不跨端硬搬 | H5 代码不能直接搬到小程序运行，必须按小程序环境改写（标签、API、存储、路由）。 |
| P-09 | 后端改动必 push | Trae /workspace 的修改必须下载到本地，由用户 push 到 GitHub，等 Vercel 部署完成后小程序才能访问到最新接口。 |
| P-10 | 模糊表述零容忍 | 禁止用"应该""大概""可能""等着就好"判断状态或操作，状态判断必须明确"已完成/未完成"。 |

### 1.4 快速索引

| 关键词 | 导航 |
|--------|------|
| 部署报错 / Vercel 500 | 第四部分 · 关键坑点 3.7 |
| 新增字段 / 改数据库 | 第三部分 · 常规迭代任务工作流 |
| 请求体丢失 / Payload 正确但后端收不到 | 第四部分 · 关键坑点 3.7 |
| 数据写不进去 / RLS | 第四部分 · 关键坑点 3.11 |
| 状态丢失 / 刷新后登录失效 | 第四部分 · 关键坑点 3.3 |
| TypeScript 编译失败 | 第四部分 · 关键坑点 3.4 |
| 白屏 / 未登录访问后台 | 第四部分 · 关键坑点 3.5 |
| 安全 / token | 第四部分 · 关键坑点 3.10 |
| Git 工作流 | 第三部分 · Git 工作流、第六部分 D |
| 版本与发布 | 第五部分 · 5.5 |
| 微信生态接入 | 第五部分 · 5.6 |
| AI 协作规范 | 第二部分 |

---

## 第二部分：AI 协作规范

### 2.1 核心原则

#### R-01 操作步骤必须可执行、可验证

任何涉及操作的回复，必须明确到：
- 在哪个页面
- 点哪个按钮
- 填什么内容
- 点完之后看到什么才算成功

禁止只写"提交后等着就好"。

#### R-02 不替用户判断"应该完成了"

用户说"做完了"时，必须反问确认：
- 你在哪个页面看到的状态？
- 那个状态旁边的文字具体是什么？
- 方便的话截个图，我帮你确认是否真的完成。

#### R-03 多平台流程必须画节点

涉及多个平台（阿里云、Vercel、微信公众平台、Supabase）时，必须写明：
- 当前在哪个节点
- 这个节点做完后，下一个节点是什么
- 每个节点的完成标志是什么

#### R-04 等待类任务必须给检查动作

涉及等待时，必须给出：
- 等什么
- 什么时候去检查
- 检查什么
- 看到什么算正常
- 看到什么算异常

#### R-05 发现截图与预期不一致时，必须当场指出

看到用户截图后，只允许说两种判断：
- "这个状态表示已完成，可以进行下一步。"
- "这个状态表示还没完成，你还需要做 XXX。"

#### R-06 禁止使用的模糊表述

以下词语禁止单独用于判断状态或操作：
- 应该
- 大概
- 可能
- 等着就好
- 没问题吧

### 2.2 沟通规范

#### R-07 操作步骤四要素模板

任何操作类回复，必须包含：

1. **在哪个页面**：`微信公众平台 → 左侧「管理」→「版本管理」`
2. **点哪个按钮**：`点击绿色按钮「提交审核」`
3. **填什么内容**：`功能截图、审核备注，可填“理发店预约小程序，包含在线预约、会员、支付”`
4. **点完之后会看到什么**：`页面顶部出现「审核版本」区域，并显示「审核中」`

#### R-08 状态确认模板

当用户说"我做完了"时，回复：

```
请告诉我：
1. 你在哪个页面看到的状态？
2. 那个状态旁边的文字具体是什么？
3. 方便的话截个图，我帮你确认是否真的完成。
```

#### R-09 多平台流程节点模板

| 节点 | 平台 | 操作 | 完成标志 |
|------|------|------|----------|
| 1 | 阿里云 DNS | 添加 CNAME 记录 | 记录状态为「启用」 |
| 2 | Vercel | 绑定域名 | 状态变绿「Valid Configuration」 |
| 3 | 微信公众平台 | 配置 request 合法域名 | 保存成功，后台显示新域名 |
| 4 | 代码 | 替换 API 地址 | 重新上传后，体验版可正常请求 |

#### R-10 等待类任务检查模板

| 等待对象 | 检查时间 | 检查动作 | 正常标志 | 异常处理 |
|----------|----------|----------|----------|----------|
| 小程序审核 | 提交后第 2 天下午 | 登录后台看「审核版本」 | 状态为「审核中」 | 若为「审核不通过」，按驳回原因修改 |
| DNS 生效 | 配置后 10 分钟 | Vercel 点 Refresh | 状态变绿 | 若仍黄色，等 30 分钟再刷新 |
| 域名备案 | 提交后 1-20 个工作日 | 登录阿里云备案控制台 | 显示「已备案」 | 若被驳回，按原因补材料 |

### 2.3 AI 回复自检清单

每次回复涉及操作前，逐条自检：

- [ ] R-01：是否写清"在哪个页面、点哪个按钮、填什么、看到什么才算成功"？
- [ ] R-02：是否默认用户已完成某个动作？
- [ ] R-03：涉及多平台流程，是否说清当前节点和下一个节点？
- [ ] R-04：涉及等待，是否给出明确检查动作和时间点？
- [ ] R-05：是否用"应该""大概"判断状态？
- [ ] R-06：是否出现禁止使用的模糊词语？
- [ ] R-07：操作步骤四要素是否齐全？
- [ ] R-08：用户说"做完了"时，是否反问了状态？
- [ ] R-09：多平台流程是否画了节点表？
- [ ] R-10：等待类任务是否给了检查模板？

### 2.4 错误追溯与改进记录

| 错误编号 | 时间 | 违反规则 | 错误描述 | 影响 | 改进措施 | 状态 |
|----------|------|----------|----------|------|----------|------|
| E-001 | 2026-09-17 | R-01、R-08 | 未告知需在网页端点「提交审核」，导致用户误以为已提交 | 时间浪费 | 后台操作必须写清四要素，并反问确认 | 已记录 |

后续所有因 AI 模糊表述、默认用户已完成、操作步骤不全导致的问题，均按此表记录并同步更新规则。

---

## 第三部分：工作流规范

### 3.1 改动后三步验证

任何代码改动完成后，必须按顺序执行：

#### 第一步：本地构建

```bash
npm run build
```

- 捕获 TypeScript 类型错误、编译错误、导入路径错误
- 通过标准：退出码为 0，终端无 `error` 级别报错

#### 第二步：本地启动后端

```bash
npm run server:api
```

- 验证后端服务能正常启动，路由、中间件、Supabase 连接无运行时错误
- 通过标准：服务启动成功，无异常堆栈，可用 curl/浏览器访问 `/api/health`

#### 第三步：浏览器/小程序 Network 检查

1. 打开页面，按 `F12` 打开 DevTools → Network 面板；
2. 操作关键功能（登录、添加客户、预约、支付）；
3. 检查对应请求的 Payload、Preview/Response、Status。

通过标准：关键请求 Payload 与 Response 一致，数据库中数据正确持久化。

### 3.2 常规迭代任务工作流

以"新增一个字段"为例，其他任务参照执行。

| 步骤 | 动作 | 关键检查 |
|------|------|----------|
| 1 需求落地 | 记录需求，明确验收标准 | 验收标准可验证、可测量 |
| 2 评估影响范围 | 数据库、后端、类型、前端表单、API 调用、种子数据 | 不遗漏依赖项 |
| 3 数据库变更 | 新增迁移文件，更新 schema.sql，Supabase 执行迁移 | RLS 策略仍然适用 |
| 4 后端实现 | 显式字段映射，不要 `toSnakeCase(body)` 入库 | 字段校验、统一返回格式 |
| 5 类型同步 | 更新 `shared/types.ts` | 前后端类型一致 |
| 6 前端实现 | 表单字段、提交结构、列表/详情展示 | 权限判断不影响新字段 |
| 7 本地验证 | 执行 3.1 三步验证 | Payload、Response、数据库三处一致 |
| 8 部署与线上实测 | push 触发 Vercel 部署，实测新增字段 | Functions 日志无异常 |
| 9 文档与复盘 | 更新宪章/状态日志 | 新问题记录到踩坑百科 |

### 3.3 紧急修复任务工作流

| 阶段 | 时间 | 动作 |
|------|------|------|
| 止血 | 5 分钟内 | 关闭/隐藏故障功能、回滚到上一个稳定版本 |
| 定位 | 15 分钟内 | 查看 Vercel Functions 日志、Network、数据库，定位根因 |
| 修复与验证 | 30 分钟内 | 本地修复并执行 3.1 三步验证 |
| 上线与观察 | 15 分钟内 | push 部署，线上实测，观察日志 |
| 复盘 | 24 小时内 | 更新宪章踩坑记录，评估是否需偿还技术债务 |

### 3.4 小程序与 H5 协同开发工作流

1. **数据同源**：三端共用同一组 `/api` 接口和同一个 Supabase 数据库；
2. **类型复用**：所有实体类型定义在 `shared/types.ts`，H5 和小程序共同 import；
3. **逻辑复用**：会员折扣、积分计算、排队等待时间等纯计算逻辑抽到 `shared/lib/`；
4. **流程一致**：预约 → 确认 → 结算 → 支付 → 评价，两端核心流程必须一致；
5. **UI 对齐**：颜色、字体、按钮、卡片、状态徽章、空状态以 H5 顾客端为基准；
6. **分别实现**：小程序不引入 Taro/Uni-app，原生实现，避免硬搬 H5 代码。

### 3.5 Git 工作流

- 一次提交一个逻辑单元，不要一次提交太多改动；
- 提交信息格式：`type: 描述`，type 包括 `feat`、`fix`、`chore`、`perf`、`docs`；
- push 前最后一次验证必须是 `npm run build`；
- 稳定版本打 tag：`git tag vX.Y.Z`；
- 出问题不知道回退到哪个版本时，用 `git log --oneline -10` 定位。

---

## 第四部分：踩坑百科

### 4.1 严重/高频坑号表

| 坑号 | 描述 | 严重度 | 章节 |
|------|------|--------|------|
| 1 | Windows GB2312 编码破坏中文文件 | ⭐ | 环境 |
| 5 | 前后端两套 mockData 导致数据不一致 | ⭐ | API |
| 9 | Zustand Store 刷新页面后数据丢失 | ⭐ | 状态 |
| 10 | localStorage 和 Zustand 不同步 | ⭐ | 状态 |
| 11 | `btoa()` 不支持中文 | ⭐ | TypeScript |
| 12 | 空数组 truthy 导致误判 | ⭐ | TypeScript |
| 20 | 敏感信息存在前端 | ⭐ | 安全 |
| 21 | Vercel Hobby 函数数量限制 | ⭐ | 部署 |
| 21.5 | API 返回 500 Internal Server Error | ⭐ | 部署 |
| 24.5 | Vercel + Express 请求体解析失败 | ⭐ | 部署 |
| 24.6 | Vercel 环境变量值被污染导致 API 500 | ⭐ | 部署 |
| 25 | 国内网络无法访问境外 API | ⭐ | 网络 |
| 31 | localStorage 存敏感信息 | ⭐ | 安全 |
| 35 | RLS 策略阻止读写 | ⭐ | 数据库 |
| 47 | 财务报表用随机假数据生成趋势和排名 | ⭐ | 业务 |
| 48 | 会员管理 500：`referral_records` 查询失败 | ⭐ | 业务 |
| 49 | 店铺设置白屏：`openingHours` 字段缺失 | ⭐ | 业务 |
| 55 | 股东身份未实现自动转化 | ⭐ | 业务 |
| 56 | 顾客端无提现入口 | ⭐ | 业务 |
| 57 | 提现状态流转与余额扣减不同步 | ⭐ | 业务 |
| 58 | `stockholder_benefit_records` 表缺失 | ⭐ | 业务 |
| 60 | Vercel 部署报 TS2339/TS2538 | ⭐ | TypeScript |
| 61 | 本地 build 不检查 api/ 目录 | ⭐ | 部署 |
| 62 | Vercel 项目 Git 绑定损坏 | ⭐ | 部署 |
| 63 | settlements 表缺字段导致 500 | ⭐ | 业务 |
| 68 | 微信支付未接入却直接标记成功 | ⭐ | 支付 |
| 69 | 微信 Native 支付商户订单号长度限制 32 位 | ⭐ | 支付 |
| 70 | 微信支付回调验签需改用公钥 | ⭐ | 支付 |
| 71 | Vercel Serverless 微信支付下单超时 | ⭐ | 支付 |
| 73 | Vercel ESM 不支持目录导入 | ⭐ | 部署 |
| 74 | `api/app.ts` 引用了不存在的路由文件 | ⭐ | 部署 |
| 75 | Supabase foreign table 查询依赖外键关系，线上无 FK 时报错 | ⭐ | 数据库 |
| 76 | 真实 API 模式 catch 降级到 mock，导致"成功但报错" | ⭐ | API |
| 77 | React 按钮同时绑定 onClick + onMouseDown 重复触发 | ⭐ | 前端 |
| 78 | 微信支付弹窗只展示占位文字，未生成真实二维码 | ⭐ | 支付 |
| 79 | 前端调用未实现的后端接口导致运行时 404 | ⭐ | 业务 |

### 4.2 关键坑点详解

#### 4.2.1 环境与编码

**坑 1：Windows GB2312 编码破坏中文文件**
- 现象：本地运行正常，push 到 Linux/Vercel 后中文变问号或乱码。
- 解决：项目根目录加 `.gitattributes`，强制 `* text=auto eol=lf`；所有文件保存为 UTF-8。

#### 4.2.2 前端构建

**坑 5：前后端两套 mockData 导致数据不一致**
- 现象：前端展示正常，后端接口返回不同结构，联调时字段对不上。
- 解决：删除前端独立 mock，统一用后端接口返回的数据结构；测试用 seed 数据。

**坑 7：Tailwind CSS 类名拼写错误无编译报错**
- 现象：样式不生效，但构建通过。
- 解决：复杂样式用 clsx + tailwind-merge 组合，关键 UI 多浏览器检查。

#### 4.2.3 状态管理

**坑 9：Zustand Store 刷新页面后数据丢失**
- 现象：登录后刷新页面，需要重新登录。
- 解决：Zustand 配合 `persist` 中间件，把关键状态持久化到 localStorage（敏感信息除外）。

**坑 10：localStorage 和 Zustand 不同步**
- 现象：多标签页同时操作，状态不一致。
- 解决：持久化层以 localStorage 为准，跨标签监听 `storage` 事件同步。

#### 4.2.4 TypeScript

**坑 11：`btoa()` 不支持中文**
- 现象：含中文的 base64 编码在另一端解码乱码。
- 解决：使用 `encodeURIComponent` + `btoa` / `atob` + `decodeURIComponent`。

**坑 12：空数组是 truthy**
- 现象：`if (arr)` 永远为 true，导致空数组时进入错误分支。
- 解决：判断数组用 `Array.isArray(arr) && arr.length > 0`。

**坑 60/61：Vercel 部署 TypeScript 报错**
- 现象：本地 `npm run check` 通过，Vercel build 时报 `TS2339`/`TS2538`。
- 解决：本地必须用 `npm run build` 验证，它才会检查 `api/` 目录；对 `unknown` 类型先用 `instanceof Error` 或类型断言。

#### 4.2.5 React 组件

**坑 15：useEffect 中 navigate 导致循环跳转**
- 现象：页面无限刷新或跳转。
- 解决：useEffect 依赖项只放必要的 state，navigate 用 `useRef` 标记是否已执行。

**坑 17.6：未登录直接访问后台路由导致白屏**
- 现象：清空缓存后访问 `/shop/xxx`，页面空白。
- 解决：路由守卫在 render 前判断登录态，未登录时跳转登录页或显示登录弹窗。

**坑 77：React 按钮同时绑定 onClick + onMouseDown 重复触发**
- 现象：登录按钮同时写了 `onClick={handleLogin}` 和 `onMouseDown={handleLogin}`，用户点击时 `handleLogin` 被连续调用两次；第一次成功登录并跳转，第二次因状态变化/网络等原因报错，错误提示覆盖到已跳转的页面上。
- 解决：表单提交和按钮点击只保留一种触发方式（通常用 `onClick` 或表单 `onSubmit`），不要为"兜底"再绑 `onMouseDown`、`onKeyDown` 等重复事件。

#### 4.2.6 API 设计

**坑 18：后端 API 返回格式不统一**
- 现象：有的接口返回 `{ data }`，有的返回 `{ success, data, error }`。
- 解决：统一返回 `{ success, data, error }`，前端封装统一错误处理。

**坑 20：敏感信息存在前端**
- 现象：API Key、私钥、数据库连接串写在前端代码或 .env 中被打包。
- 解决：敏感信息只放在服务端环境变量；前端通过后端接口间接调用第三方服务。

**坑 76：真实 API 模式 catch 降级到 mock，导致"成功但报错"**
- 现象：生产环境 `VITE_USE_REAL_API=true` 时，后端登录接口返回 `{"success":false,"error":"手机号或密码错误"}`，前端 catch 后降级到 mock 登录成功，用户看到页面已跳转但弹窗仍显示报错。
- 解决：真实 API 模式下失败必须直接抛出后端错误并显示给用户，禁止静默降级到 mock；mock 只应在明确禁用真实 API 时生效。

**坑 79：前端调用未实现的后端接口导致运行时 404**
- 现象：前端 `src/api.ts` 已写好 `/vip-configs/*` 调用，但后端 `api/routes/index.ts` 未挂载对应路由，页面点击保存后返回 404，功能不可用。
- 解决：新增前端 API 封装前，先确认后端路由已实现；前后端接口变更必须同步提交；本地 `npm run server:api` 启动后用 curl/浏览器实测接口是否可达。

#### 4.2.7 部署与运维

**坑 21：Vercel Hobby 函数数量限制**
- 现象：部署报错 `Serverless Function count exceeded`。
- 解决：`vercel.json` 配置单一函数入口，把所有路由合并到一个 `api/index.ts`。

**坑 21.5：API 返回 500 Internal Server Error**
- 现象：Vercel Functions 日志显示函数启动失败或运行时错误。
- 解决：本地 `npm run build` + `npm run server:api` 启动后实测；Vercel Functions 日志查看具体堆栈。

**坑 22：环境变量未在部署平台设置**
- 现象：本地正常，线上报错 `SUPABASE_URL is required`。
- 解决：Vercel Project Settings → Environment Variables 里配置所有 `.env` 变量。

**坑 24.5：Vercel + Express 请求体解析失败**
- 现象：本地 POST 正常，Vercel 上 `req.body` 为空。
- 解决：Vercel Runtime 下用自定义 body parser 读取 `req.rawBody` 或 `Buffer.concat` 手动解析 JSON。

**坑 24.6：Vercel 环境变量值被污染导致 API 500**
- 现象：环境变量包含多余空格、引号或换行符。
- 解决：Vercel 后台重新粘贴环境变量值，本地打印验证；敏感变量不要带引号。

**坑 62：Vercel 项目 Git 绑定损坏**
- 现象：Connect 显示成功但 GitHub webhook 为空，push 不触发部署。
- 解决：Vercel 里 Disconnect 后重新 Connect，或重建项目。

**坑 73：Vercel ESM 不支持目录导入**
- 现象：本地 `npm run build` 通过，Vercel build 时报 `ERR_UNSUPPORTED_DIR_IMPORT: Directory import '/var/task/server/db' is not supported resolving ES modules`。
- 根因：Vercel Runtime 使用原生 ESM，所有相对路径导入必须显式写 `.js` 扩展名，不能写 `../db`，必须写 `../db.js`。
- 解决：所有 `import` 和 `require` 的相对路径都补全 `.js`；项目加 CI 或在本地用 `npm run build` 验证时额外用 `npx tsc -p api/tsconfig.json` 检查 api/ 目录。

**坑 74：`api/app.ts` 引用了不存在的路由文件**
- 现象：Vercel build 时报 `error TS2307: Cannot find module '../server/routes/upload.js' or its corresponding type declarations`。
- 根因：`api/app.ts` 里 `import uploadRouter from '../server/routes/upload.js'`，但项目里没有 `server/routes/upload.ts` 或 `upload.js`；可能是文件被误删、重命名，或 PR 合并时遗漏。
- 解决：补充缺失的路由文件；本地用 `npm run build` 验证通过后再 push；删除引用前先确认该路由是否还有用途。

**坑 78：微信支付弹窗只展示占位文字，未生成真实二维码**
- 现象：开单结算选择微信支付后，弹窗只显示"二维码占位"文字，顾客无法扫码付款；或后端已返回 `code_url`，但前端未将其转换为二维码图片。
- 解决：引入 `qrcode` 库，使用 `QRCode.toDataURL(codeUrl)` 将微信 `code_url` 渲染为真实二维码图片；在 `useEffect` 中监听 `codeUrl` 变化并处理取消竞争。

#### 4.2.8 网络与环境

**坑 25：国内网络无法访问境外 API**
- 现象：dicebear 头像、某些 CDN 在国内加载失败或极慢。
- 解决：第三方服务必须确认国内可访问；头像等静态资源改用国内图床或本地兜底。

#### 4.2.9 安全

**坑 31：localStorage 存敏感信息**
- 现象：token、密码存在 localStorage，XSS 风险。
- 解决：敏感 token 用 httpOnly cookie；当前顾客端 `customerId` 明文传参属于已知技术债务，待改造。

#### 4.2.10 Supabase 数据库

**坑 35：RLS 策略阻止读写**
- 现象：前端请求返回 400 或数据写不进去， Supabase 无报错。
- 解决：每新建表必须配置 RLS 策略；测试时先关闭 RLS 确认是否是策略问题。

**坑 37：数据库 Schema 和前端类型不同步**
- 现象：前端提交字段名与数据库列名不一致，导致假性保存。
- 解决：修改 Schema 后同步更新 `shared/types.ts` 和前端表单；迁移脚本必须执行。

**坑 41：Schema 变更没有迁移脚本**
- 现象：本地表结构和线上不一致，代码在线上报错。
- 解决：每次改表在 `migrations/` 新增迁移文件，并在 Supabase SQL Editor 执行。

**坑 75：Supabase foreign table 查询依赖外键关系，线上无 FK 时报错**
- 现象：本地开发用 Supabase 自动 foreign table 查询（如 `select('*, coupons(*)')`）正常，但线上生产数据库未建立外键关系时，同样查询返回 `could not find the 'coupons' column` 或 `foreign key relationship not found`，导致接口 500。
- 解决：生产环境避免依赖 foreign table 自动关联；改为先查主表，再用 `in` 手动查询关联表，最后在前端/后端组装数据。新增表时如需要 foreign table 查询，必须同时确认线上已建立 FK 关系。

#### 4.2.11 近期专项记录（索引）

| 专项 | 内容 | 关键坑号 |
|------|------|----------|
| 财务报表假数据 | 趋势图和发型师业绩排名用 `Math.random()` 生成 | 47 |
| 会员管理 500 | `/api/referrals` 查询 `referral_records` 失败 | 48 |
| 店铺设置白屏 | `openingHours` 字段缺失 | 49 |
| 员工自助修改头像与手机号 | 头像上传、手机号修改权限 | 50-51 |
| 预约权限控制与发型师调配 | 权限散落、stylist/barber 命名混用 | 52-54 |
| 股东自动转化与提现管理 | 推荐奖励、提现状态机 | 55-58 |
| Vercel TypeScript 报错兜底 | 后端类型检查 | 60-61 |
| Vercel Git 绑定损坏 | 项目重建 | 62 |
| 结算、图表与小程序组件 | settlements 字段、Recharts、mbs-icon | 63-67 |
| 微信支付真实 Native 支付 | 商户订单号、公钥验签、超时 | 68-72 |
| 2026-09-26 自查修复专项 | foreign table、登录重复触发、mock 降级、二维码占位、未实现接口 | 75-79 |

---

## 第五部分：治理机制

### 5.1 需求变更管理

#### 5.1.1 需求变更流程

任何新需求必须走以下四步：

1. **记录**：在任务书/计划书中写明需求背景、目标用户、验收标准；
2. **评估**：评估对数据库、后端、前端、测试、部署的影响范围；
3. **决策**：确认是否在本次范围；超出范围的需求另开任务；
4. **执行**：按 3.2 常规迭代任务工作流执行。

#### 5.1.2 MVC（最小可行版本）原则

- 先做能跑通的最小版本；
- 不提前为假设的未来需求做抽象；
- 三个相似代码行优于一个 premature abstraction。

#### 5.1.3 范围蔓延的红色信号

- "顺便把 XXX 也改一下"
- "以后可能会用到"
- "加个开关吧，虽然这次不用"
- 这些都需要单独评估，不能顺手做。

### 5.2 团队协作规范

#### 5.2.1 给 AI 发指令的标准模板

```
任务：[一句话说清楚要做什么]

背景：[当前是什么现象？已经尝试过什么？有没有报错？]

限制：[不要改哪些文件/功能？范围边界是什么？]

验收标准：[怎么算完成？需要看到什么结果？]
```

#### 5.2.2 报错信息提交格式

```
- 错误位置：哪个文件 / 哪一行 / 哪个接口
- 错误原因：一句话解释
- 解决方案：分步骤说明
- 我需要执行的命令：直接可复制
```

#### 5.2.3 项目主人工作流（铁律）

1. AI 在 Trae `/workspace` 生成/修改代码；
2. 项目主人下载到本地 Windows 电脑的 `E:\MBS`；
3. 本地运行 `npm run dev` 测试；
4. 测试通过后从本地 push 到 GitHub；
5. Vercel 自动从 GitHub 部署。

绝对不要：
- 让 AI 在 Trae 里 push；
- 说"Vercel 自动部署了你刷新一下"，除非代码已经到 GitHub；
- 假设 Trae `/workspace` 的改动会自动同步到本地或线上。

### 5.3 回滚决策流程

#### 5.3.1 回滚触发条件

- 核心功能不可用（登录失败、无法预约、无法支付）；
- 线上出现数据错误 or 数据丢失；
- 部署后 30 分钟内收到多个用户报错。

#### 5.3.2 回滚命令

```bash
git log --oneline -10              # 找到上一个稳定版本 commit
git revert <commit-hash>           # 安全回滚，保留历史
# 或
git checkout <commit-hash> -- .    # 局部回滚特定文件
```

#### 5.3.3 回滚原则

- 先止血，再定位，再修复；
- 回滚后必须在测试环境复现问题；
- 修复后更新宪章踩坑记录。

### 5.4 技术债务管理

#### 5.4.1 当前已知技术债务

| 债务项 | 影响 | 偿还计划 |
|--------|------|----------|
| 顾客端 `customerId` 明文传参 | 安全风险 | 小程序资质到位后改造为顾客 token 鉴权 |
| stylistId / barberId 两套命名 | 代码混乱、展示不同步 | 逐步统一为 `stylistId` |
| 权限判断逻辑散落在各处 | 新增角色易漏改 | 抽象统一权限工具函数 |
| H5 与小程序部分 UI 未完全对齐 | 三端不一致 | 按 3.4 协同工作流逐步对齐 |
| 无自动化测试 | 改一处坏十处 | 核心 API 逐步补测试 |

#### 5.4.2 重构触发条件

- 同一处代码第三次修改时；
- 新增功能需要复制粘贴大量旧代码时；
- 某个坑反复出现第三次时。

### 5.5 版本与发布

#### 5.5.1 版本号规则

| 版本位 | 含义 | 示例 |
|--------|------|------|
| MAJOR | 不兼容的 API 或数据变更 | v2.0 |
| MINOR | 向后兼容的功能新增 | v2.1 |
| PATCH | 向后兼容的问题修复 | v2.1.1 |

#### 5.5.2 发布前检查清单

- [ ] 当前分支已通过 `npm run build`；
- [ ] 本地后端 `npm run server:api` 能正常启动；
- [ ] 浏览器/小程序实测核心流程；
- [ ] 数据库迁移脚本（如有）已执行并验证；
- [ ] Vercel 环境变量已配置；
- [ ] `vercel.json` 未超过免费版 Function 数量限制；
- [ ] 已知技术债务已更新；
- [ ] 已打 tag：`git tag vX.Y.Z`。

#### 5.5.3 发布后验证

1. 确认 Vercel Deployment 为 Ready；
2. 强制刷新页面/重新打开小程序；
3. 实测登录、添加数据、核心业务流程；
4. 检查 Vercel Functions Logs 无 500；
5. 如发现核心功能不可用，立即执行 5.3 回滚流程。

### 5.6 微信生态接入规范

#### 5.6.1 总体策略

- **小程序优先支付**：交易闭环优先在小程序内跑通；
- **公众号做宣传与预约入口**：内容传播、预约引导、消息触达；
- **H5 保留非支付功能**：预约、排队、查看套餐、会员信息、评价；
- **三端数据打通**：共用同一套后端 API 和 Supabase 数据库。

#### 5.6.2 接入前提

| 约束项 | 责任方 |
|--------|--------|
| 微信开放平台账号（已认证） | 店铺端 |
| 微信支付商户号 | 店铺端 |
| 微信小程序（已认证） | 店铺端 |
| 域名与 ICP 备案 | 店铺端 |
| 后端 API HTTPS | 开发端/部署平台 |
| 顾客身份打通（手机号+unionid） | 开发端 |

#### 5.6.3 数据打通原则

1. 手机号优先匹配同一顾客；
2. unionid 辅助关联身份；
3. 一份顾客数据，三端一致；
4. 登录态隔离，识别同一顾客。

#### 5.6.4 当前状态

- [x] 微信渠道接入策略已确定；
- [x] `customers` 表已扩展微信字段；
- [x] 小程序基础框架已搭建；
- [x] 小程序 UI 与 H5 统一改造完成；
- [x] 真实微信 Native 支付已接入并通过 0.01 元实测；
- [ ] 顾客端 token 鉴权改造 pending 资质到位后启动。

详细操作步骤见 [`微信渠道接入规划与指南.md`](file:///workspace/微信渠道接入规划与指南.md)。

---

## 第六部分：附录

### A. 新项目避坑 Checklist

#### 项目初始化
- [ ] `.gitignore` 包含 `node_modules`、`.env`、`dist`；
- [ ] `.gitattributes` 解决 CRLF 问题；
- [ ] `package-lock.json` 已提交；
- [ ] `tsconfig.json` paths alias 已配置。

#### 前端状态管理
- [ ] 状态管理方案确定（Zustand + persist）；
- [ ] localStorage 和 Zustand 同步；
- [ ] API 失败有 fallback。

#### API 设计
- [ ] 统一返回格式 `{ success, data, error }`；
- [ ] 统一错误处理；
- [ ] 敏感信息不在前端。

#### 部署
- [ ] `.env` 在部署平台配置；
- [ ] 了解部署平台免费额度；
- [ ] 部署后实测 POST/PUT 请求体解析。

#### 安全
- [ ] API 有 rate limiting；
- [ ] 敏感数据不用 localStorage；
- [ ] 第三方 API 国内可访问。

#### 数据库
- [ ] 有 `migrations/` 目录；
- [ ] 种子数据脚本可跑通；
- [ ] RLS 策略已配置。

#### 错误监控与测试
- [ ] 接入错误监控（Sentry 等）；
- [ ] 至少有一个核心 API 测试；
- [ ] 前端有全局 error boundary。

#### 项目文档
- [ ] README 包含环境要求、启动步骤、部署步骤、FAQ；
- [ ] 踩坑记录文档持续维护。

### B. 常用命令速查

#### 项目启动与构建
```bash
npm install           # 安装依赖
npm run dev           # 启动开发服务器
npm run build         # 本地构建
npm run server:api    # 启动本地后端 API
npm run seed-db       # 导入种子数据到 Supabase
```

#### Git 工作流
```bash
git status                        # 查看当前改动
git add <file>                    # 添加特定文件
git commit -m "type: 描述"        # 提交
git log --oneline -10             # 查看最近 10 次提交
git tag vX.Y.Z                    # 打版本标签
git push origin main --tags       # 推送代码和标签
```

#### 部署与运维
```bash
vercel --prod                     # 手动触发生产部署（如已安装 CLI）
vercel logs <deployment-url>      # 查看部署日志
```

#### 后端调试
```bash
# 本地测试 API
curl -X POST http://localhost:3000/api/customers \
  -H "Content-Type: application/json" \
  -d '{"name":"测试","phone":"13800000001"}'

# 健康检查
curl http://localhost:3000/api/health
```

#### 浏览器/小程序快捷操作
```bash
Ctrl+Shift+R        # Windows / Linux：强制刷新
Cmd+Shift+R         # Mac：强制刷新
```

### C. 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | — | 初始版本：线性叙事结构 |
| v2.0 | 2026-06-26 | 重构为五层金字塔结构 |
| v3.0 | 2026-09-01 | 微信支付真实 Native 支付接入完成 |
| v4.0 | 2026-09-17 | 精简结构，新增 AI 协作规范（R-01 ~ R-10），强化状态确认和操作流程四要素，删除重复内容 |
| v4.1 | 2026-09-26 | 新增坑 75-79（foreign table 查询、mock 降级、按钮重复触发、二维码占位、未实现接口）；补充 2026-09-26 自查修复专项索引 |

---

*文件结束。*
